local a=loadstring(game:HttpGet("https://raw.githubusercontent.com/itavvietivvoyd-ux/-/refs/heads/main/%D8%A8%D8%AF%D9%88%D9%86%20.txt"))()task.wait(8)print("Done Loading!")local a=loadstring(game:HttpGet("https://raw.githubusercontent.com/tbao143/Library-ui/refs/heads/main/Redzhubui"))()local b=a:MakeWindow({Title="KIRA | Brookhaven RP  1.0",SubTitle="By KIRA | RIP",SaveFolder=" update 1.0"})b:AddMinimizeButton({Button={Image="rbxassetid://87873786120601",BackgroundTransparency=0},Corner={CornerRadius=UDim.new(35,1)}})local c=b:MakeTab({"المعلومات","info"})local d=b:MakeTab({"متعه","fun"})local e=b:MakeTab({"سكنات","shirt"})local f=b:MakeTab({"مضادات","shield"})local g=b:MakeTab({"كلشي ريمبو","brush"})local h=b:MakeTab({"تخريب عل سيارات","Car"})local i=b:MakeTab({Title="التجميد",Icon="rbxassetid://10723414308"})local j=b:MakeTab({"تخريب عل لاعبين","skull"})local k=b:MakeTab({"تخريبات","star"})local l=b:MakeTab({"شادرات","sunmoon"})local m=b:MakeTab({"اطفال","baby"})local n=b:MakeTab({"تنقلات","map-pin"})c:AddSection({"معلومات"})c:AddDiscordInvite({Name="KIRA HUB",Description="حسابي تيك",Logo="rbxassetid://87873786120601",Invite="8b6p_"})local function o()if identifyexecutor then return identifyexecutor()elseif syn then return"Synapse X"elseif KRNL_LOADED then return"KRNL"elseif is_sirhurt_closure then return"SirHurt"elseif pebc_execute then return"ProtoSmasher"elseif getexecutorname then return getexecutorname()else return"Executor Desconhecido"end end;local p=o()local q=c:AddParagraph({"المنفذ",p})local r=c:AddSection({"update  1.0"})local q=c:AddParagraph({"KIRA","to update"})c:AddButton({Name="انسخ حسابي تلي",Callback=function()setclipboard("@X_th4")setclipboard("X_th4")end})local r=d:AddSection({"تحكم ب شخصيه"})d:AddSlider({Name="سرعه",Increase=1,MinValue=16,MaxValue=888,Default=16,Callback=function(s)local t=game.Players.LocalPlayer;local u=t.Character or t.CharacterAdded:Wait()local v=u:FindFirstChildOfClass("Humanoid")if v then v.WalkSpeed=s end end})d:AddSlider({Name="القفز",Increase=1,MinValue=50,MaxValue=500,Default=50,Callback=function(s)local t=game.Players.LocalPlayer;local u=t.Character or t.CharacterAdded:Wait()local v=u:FindFirstChildOfClass("Humanoid")if v then v.JumpPower=s end end})d:AddSlider({Name="جاذبيه",Increase=1,MinValue=0,MaxValue=10000,Default=196.2,Callback=function(s)game.Workspace.Gravity=s end})local w=false;game:GetService("UserInputService").JumpRequest:Connect(function()if w then local u=game.Players.LocalPlayer.Character;if u and u:FindFirstChild("Humanoid")then u.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)end end end)d:AddButton({Name="ريستات للسرعه و القفز و الجاذبيه",Callback=function()local t=game.Players.LocalPlayer;local u=t.Character or t.CharacterAdded:Wait()local v=u:FindFirstChildOfClass("Humanoid")if v then v.WalkSpeed=16;v.JumpPower=50 end;game.Workspace.Gravity=196.2;w=false end})d:AddToggle({Name="قفز لانهائي",Default=false,Callback=function(s)w=s end})local x={Enabled=false,Connections={},SoccerBalls={}}local y=game:GetService("Players")local z=game:GetService("RunService")local A=game:GetService("Workspace")local B=y.LocalPlayer;local function C(u)if not u then return end;for D,E in ipairs(u:GetDescendants())do if E:IsA("BasePart")then E.CanCollide=not x.Enabled;E.Anchored=false end end end;local function F(G)if G.Position.Y<-500 then local H=CFrame.new(0,100,0)local I=RaycastParams.new()I.FilterDescendantsInstances={B.Character}local J=A:Raycast(G.Position,Vector3.new(0,500,0),I)G.CFrame=J and CFrame.new(J.Position+Vector3.new(0,5,0))or H end end;local function K()local L=A:FindFirstChild("Com",true)and A.Com:FindFirstChild("001_SoccerBalls")if L then for D,M in ipairs(L:GetChildren())do if M.Name:match("^Soccer")then pcall(function()M.CanCollide=not x.Enabled;M.Anchored=x.Enabled end)x.SoccerBalls[M]=true end end;if not x.Connections.BallAdded then x.Connections.BallAdded=L.ChildAdded:Connect(function(M)if M.Name:match("^Soccer")then task.wait(0.3)pcall(function()M.CanCollide=not x.Enabled;M.Anchored=x.Enabled end)end end)end end end;local function N()x.Connections.Heartbeat=z.Heartbeat:Connect(function()local u=B.Character;if u then C(u)local G=u:FindFirstChild("HumanoidRootPart")if G then F(G)end end;if tick()%2<0.1 then K()end end)end;local O=d:AddToggle({Name="نوكليب",Description="",Default=false})O:Callback(function(P)x.Enabled=P;if P then N()K()x.Connections.CharAdded=B.CharacterAdded:Connect(function()task.wait(0.5)C(B.Character)end)else for D,Q in pairs(x.Connections)do Q:Disconnect()end;if B.Character then C(B.Character)end;for M in pairs(x.SoccerBalls)do if M.Parent then pcall(function()M.CanCollide=true;M.Anchored=false end)end end end end)local y=game:GetService("Players")local z=game:GetService("RunService")local R={}local S={}local T=false;local U="RGB Suave"d:AddButton({Name="طيران",Description="",Callback=function()local V,D=pcall(function()loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fly-gui-v3-30439"))()end)game.StarterGui:SetCore("SendNotification",{Title=V and"Sucesso"or"Erro",Text=V and"Fly GUI carregado!"or"Falha ao carregar o Fly GUI.",Duration=5})end})local r=d:AddSection({"كاشف"})d:AddDropdown({Name="كاشف",Default="ريمبو ",Options={"ريمبو","ابيض","اسود","احمر","اخضر","ازرك","اصفر","وردي","بنفسجي"},Callback=function(W)U=W end})local function X()if U=="ريمبو"then local Y=tick()%5/5;return Color3.fromHSV(Y,1,1)elseif U=="اسود"then return Color3.fromRGB(0,0,0)elseif U=="ابيض"then return Color3.fromRGB(255,255,255)elseif U=="احمر"then return Color3.fromRGB(255,0,0)elseif U=="اخضر"then return Color3.fromRGB(0,255,0)elseif U=="ازرك"then return Color3.fromRGB(0,170,255)elseif U=="اصفر"then return Color3.fromRGB(255,255,0)elseif U=="وردي"then return Color3.fromRGB(255,105,180)elseif U=="بنفسجي"then return Color3.fromRGB(128,0,128)end;return Color3.new(1,1,1)end;local function Z(t)if t==y.LocalPlayer then return end;if not T then return end;local u=t.Character;if u then local _=u:FindFirstChild("Head")if _ then if R[t]then R[t]:Destroy()end;local a0=Instance.new("BillboardGui")a0.Name="ESP_Billboard"a0.Parent=_;a0.Adornee=_;a0.Size=UDim2.new(0,200,0,50)a0.StudsOffset=Vector3.new(0,3,0)a0.AlwaysOnTop=true;local a1=Instance.new("TextLabel")a1.Name="TextLabel"a1.Parent=a0;a1.Size=UDim2.new(1,0,1,0)a1.BackgroundTransparency=1;a1.TextStrokeTransparency=0.5;a1.Font=Enum.Font.SourceSansBold;a1.TextSize=14;a1.Text=t.Name.." | "..t.AccountAge.." dias"a1.TextColor3=X()R[t]=a0 end end end;local function a2(t)if R[t]then R[t]:Destroy()R[t]=nil end end;local a3=d:AddToggle({Name="تشغيل كاشف",Description="",Default=false})a3:Callback(function(W)T=W;if T then for D,t in pairs(y:GetPlayers())do Z(t)end;local a4=z.Heartbeat:Connect(function()for D,t in pairs(y:GetPlayers())do Z(t)end;if U=="RGB"then for D,t in pairs(y:GetPlayers())do local a5=R[t]if a5 and a5:FindFirstChild("TextLabel")then a5.TextLabel.TextColor3=X()end end end end)table.insert(S,a4)local a6=y.PlayerAdded:Connect(function(t)Z(t)local a7=t.CharacterAdded:Connect(function()Z(t)end)table.insert(S,a7)end)table.insert(S,a6)local a8=y.PlayerRemoving:Connect(function(t)a2(t)end)table.insert(S,a8)else for D,t in pairs(y:GetPlayers())do a2(t)end;for D,Q in pairs(S)do Q:Disconnect()end;S={}R={}end end)local r=e:AddSection({"نسخ سكن"})local y=game:GetService("Players")local B=y.LocalPlayer;local a9=game:GetService("ReplicatedStorage")local aa=a9:WaitForChild("Remotes")local ab;local ac=nil;local function ad()local ae={}for D,t in ipairs(y:GetPlayers())do if t.Name~=B.Name then table.insert(ae,t.Name)end end;return ae end;local af=e:AddDropdown({Name="اختر لاعب",Description="",Options=ad(),Default="",Flag="player list",Callback=function(ag)ab=ag;ac=ag end})local function ah()af:Set(ad())end;ah()e:AddButton({"تحديث قائمه",function()ah()end})y.PlayerAdded:Connect(ah)y.PlayerRemoving:Connect(ah)e:AddButton({Name="ابدا ب نسخ السكن",Callback=function()if not ac then return end;local ai=y.LocalPlayer;local aj=ai.Character;local ak=y:FindFirstChild(ac)if ak and ak.Character then local al=aj and aj:FindFirstChildOfClass("Humanoid")local am=ak.Character:FindFirstChildOfClass("Humanoid")if al and am then local an=al:GetAppliedDescription()for D,ao in ipairs(an:GetAccessories(true))do if ao.AssetId and tonumber(ao.AssetId)then aa.Wear:InvokeServer(tonumber(ao.AssetId))task.wait(0.2)end end;if tonumber(an.Shirt)then aa.Wear:InvokeServer(tonumber(an.Shirt))task.wait(0.2)end;if tonumber(an.Pants)then aa.Wear:InvokeServer(tonumber(an.Pants))task.wait(0.2)end;if tonumber(an.Face)then aa.Wear:InvokeServer(tonumber(an.Face))task.wait(0.2)end;local ap=am:GetAppliedDescription()local aq={[1]={[1]=ap.Torso,[2]=ap.RightArm,[3]=ap.LeftArm,[4]=ap.RightLeg,[5]=ap.LeftLeg,[6]=ap.Head}}aa.ChangeCharacterBody:InvokeServer(unpack(aq))task.wait(0.5)if tonumber(ap.Shirt)then aa.Wear:InvokeServer(tonumber(ap.Shirt))task.wait(0.3)end;if tonumber(ap.Pants)then aa.Wear:InvokeServer(tonumber(ap.Pants))task.wait(0.3)end;if tonumber(ap.Face)then aa.Wear:InvokeServer(tonumber(ap.Face))task.wait(0.3)end;for D,ar in ipairs(ap:GetAccessories(true))do if ar.AssetId and tonumber(ar.AssetId)then aa.Wear:InvokeServer(tonumber(ar.AssetId))task.wait(0.3)end end;local as=ak.Character:FindFirstChild("Body Colors")if as then aa.ChangeBodyColor:FireServer(tostring(as.HeadColor))task.wait(0.3)end;if tonumber(ap.IdleAnimation)then aa.Wear:InvokeServer(tonumber(ap.IdleAnimation))task.wait(0.3)end;local at=ak:FindFirstChild("PlayersBag")if at then if at:FindFirstChild("RPName")and at.RPName.Value~=""then aa.RPNameText:FireServer("RolePlayName",at.RPName.Value)task.wait(0.3)end;if at:FindFirstChild("RPBio")and at.RPBio.Value~=""then aa.RPNameText:FireServer("RolePlayBio",at.RPBio.Value)task.wait(0.3)end;if at:FindFirstChild("RPNameColor")then aa.RPNameColor:FireServer("PickingRPNameColor",at.RPNameColor.Value)task.wait(0.3)end;if at:FindFirstChild("RPBioColor")then aa.RPNameColor:FireServer("PickingRPBioColor",at.RPBioColor.Value)task.wait(0.3)end end end end end})local r=e:AddSection({"سكنات 3D"})local a9=game:GetService("ReplicatedStorage")local au={}au.ReplicatedStorage=a9;function au:MostrarNotificacao(av)pcall(function()game:GetService("StarterGui"):SetCore("SendNotification",{Title="Aviso",Text=av,Duration=5})end)end;au.Avatares={{Nome="جاتو تي مانجا",ID=124948425515124},{Nome="تونغ ساهور",ID=117098257036480},{Nome="تراليتو",ID=99459753608381},{Nome="مونستر S.A",ID=123609977175226},{Nome="قطار توماس",ID=80468697076178},{Nome="دينصور برتقالي",ID=11941741105},{Nome="بوي ايدوس",ID=15742966010},{Nome="كوهيلو",ID=71797333686800},{Nome="هيبو بو تامو",ID=73215892129281},{Nome="راتاتوي",ID=108557570415453},{Nome="جالينها",ID=71251793812515},{Nome="بيبا",ID=92979204778377},{Nome="بطريق",ID=94944293759578},{Nome="سيد",ID=87442757321244},{Nome="بوغا قراندي",ID=111436158728716},{Nome="شريك مالدياكو",ID=120960401202173},{Nome="موسكتي قراندي",ID=108052868536435},{Nome="نوب",ID=106596990206151},{Nome="باتو",ID=135132836238349},{Nome="فيل فراوله بعمر ستينات",ID=14533748491},{Nome="قلم اصفر",ID=18994959003},{Nome="ابو العضلات ",ID=135861229178903},{Nome="الاخضر",ID=75125632205005},{Nome="نعامه",ID=96931373056689},{Nome="حمار",ID=129443208066000}}function au:GetAvatarNames()local aw={}for D,ax in ipairs(self.Avatares)do table.insert(aw,ax.Nome)end;return aw end;function au:EquiparAvatar(ay)for D,ax in ipairs(self.Avatares)do if ax.Nome==ay then local az={ax.ID}pcall(function()return self.ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Wear"):InvokeServer(unpack(az))end)return end end end;local aA=e:AddDropdown({Name="سكنات",Description="",Default=nil,Options=au:GetAvatarNames(),Callback=function(aB)_G.SelectedAvatar=aB end})e:AddButton({Name="تطبيق ",Description="عند ما تدوس الزر يطبق السكن",Callback=function()if not _G.SelectedAvatar or _G.SelectedAvatar==""then au:MostrarNotificacao("Nenhum avatar selecionado!")return end;au:EquiparAvatar(_G.SelectedAvatar)end})local r=e:AddSection({"قريبا"})local aC;local aD;local aE="1Gu1n"local aF=game:GetService("ReplicatedStorage"):WaitForChild("RE")local function aG()local aH=aF:FindFirstChild(aE)if aH then pcall(function()aC=aH:Clone()aD=aH.Parent;aH:Destroy()end)end end;local function aI()if aC and aD then pcall(function()aC.Parent=aD end)aC=nil;aD=nil end end;local r=f:AddSection({"حمايه لاعب"})f:AddToggle({Name="مضاد تجميد",Description="",Default=false,Callback=function(ar)if ar then aG()conexaoAutoRemocao=aF.ChildAdded:Connect(function(aJ)if aJ.Name==aE then task.wait(0.1)pcall(function()aC=aJ:Clone()aD=aJ.Parent;aJ:Destroy()end)end end)else if conexaoAutoRemocao then conexaoAutoRemocao:Disconnect()conexaoAutoRemocao=nil end;aI()end end})local r=f:AddSection({"حمايه لاعب من تجميدات"})local y=game:GetService("Players")local a9=game:GetService("ReplicatedStorage")local aK=y.LocalPlayer;_G.__AutoAntiBang=_G.__AutoAntiBang or{enabled=false,thread=nil}local function aL()local aM=1500;for D,aJ in pairs(workspace:GetDescendants())do if aJ:IsA("BasePart")and aJ.Position.Y>aM then aM=aJ.Position.Y end end;return aM+60 end;local function aN()local aO=aK.Character;if not aO then return end;local aP=aO:FindFirstChild("HumanoidRootPart")if not aP then return end;local aQ=aP.CFrame;local aR=aL()aP.CFrame=CFrame.new(aQ.X,aR,aQ.Z)task.wait(0.15)aP.CFrame=aQ;task.wait(0.1)end;local function aS()_G.__AutoAntiBang.enabled=true;local aT=nil;while _G.__AutoAntiBang.enabled do local aO=aK.Character;if aO then local aP=aO:FindFirstChild("HumanoidRootPart")if aP then local aU=aP.Position.Y;if aT and math.abs(aU-aT)>50 then print("🚨 Bang Detected! Anti-Bang Activated.")aN()end;aT=aU end end;task.wait(0.2)end end;f:AddToggle({Name="مضاد بانج",Default=true,Callback=function(aV)if aV then if not _G.__AutoAntiBang.enabled then _G.__AutoAntiBang.thread=task.spawn(aS)print("✅ Auto Anti-Bang Started")end else _G.__AutoAntiBang.enabled=false;print("🛑 Auto Anti-Bang Stopped")end end})local r=f:AddSection({"حمايه لاعب من البانج"})f:AddToggle({Name="مضاد جلوس V1",Description="",Default=false,Callback=function(P)antiSitEnabled=P;local B=game:GetService("Players").LocalPlayer;if P then local function aW(u)local v=u:FindFirstChild("Humanoid")if v then v.Sit=false;v:SetStateEnabled(Enum.HumanoidStateType.Seated,false)if antiSitConnection then antiSitConnection:Disconnect()end;antiSitConnection=v.Seated:Connect(function(aX)if aX then v.Sit=false;v:ChangeState(Enum.HumanoidStateType.GettingUp)end end)end end;if B.Character then aW(B.Character)end;local aY;aY=B.CharacterAdded:Connect(function(u)if not antiSitEnabled then aY:Disconnect()return end;local v=u:WaitForChild("Humanoid",5)if v then aW(u)end end)else if antiSitConnection then antiSitConnection:Disconnect()antiSitConnection=nil end;if B.Character then local v=B.Character:FindFirstChild("Humanoid")if v then v:SetStateEnabled(Enum.HumanoidStateType.Seated,true)end end end end})local r=f:AddSection({"الحمايه من فلنقات ب سيارات"})f:AddToggle({Name="مضاد جلوس V2",Description="",Default=false,Callback=function(P)antiSitEnabled=P;local B=game:GetService("Players").LocalPlayer;if P then local function aW(u)local v=u:FindFirstChild("Humanoid")if v then v.Sit=false;v:SetStateEnabled(Enum.HumanoidStateType.Seated,false)if antiSitConnection then antiSitConnection:Disconnect()end;antiSitConnection=v.Seated:Connect(function(aX)if aX then v.Sit=false;v:ChangeState(Enum.HumanoidStateType.GettingUp)end end)end end;if B.Character then aW(B.Character)end;local aY;aY=B.CharacterAdded:Connect(function(u)if not antiSitEnabled then aY:Disconnect()return end;local v=u:WaitForChild("Humanoid",5)if v then aW(u)end end)else if antiSitConnection then antiSitConnection:Disconnect()antiSitConnection=nil end;if B.Character then local v=B.Character:FindFirstChild("Humanoid")if v then v:SetStateEnabled(Enum.HumanoidStateType.Seated,true)end end end end})local r=f:AddSection({"لضمان حمايتك "})local r=g:AddSection({"عداد سرعه مسئول على كل شي ريمبو"})local aZ=1;g:AddSlider({Name=" حدد سرعه",Description="سرعه تغير لون",Min=1,Max=5,Increase=1,Default=3,Callback=function(s)aZ=s end})local function a_(b0)local Y=tick()*b0%5/5;return Color3.fromHSV(Y,1,1)end;local function b1(b2,az)local b3=game:GetService("ReplicatedStorage"):FindFirstChild("RE")if b3 and b3:FindFirstChild(b2)then pcall(function()b3[b2]:FireServer(unpack(az))end)end end;local r=g:AddSection({"كلشي ريمبو"})local b4=false;g:AddToggle({Name="اسم ريمبو ",Default=false,Callback=function(P)b4=P;if P then task.spawn(function()while b4 and B.Character do local b5=a_(aZ)b1("1RPNam1eColo1r",{"PickingRPNameColor",b5})b1("1RPNam1eColo1r",{"PickingRPBioColor",b5})task.wait(0.03)end end)end end})local r=g:AddSection({"بيت ريمبو"})local b6=g:AddToggle({Name="بيت ريمبو",Description="",Default=false})b6:Callback(function(s)getgenv().rgbCasa=s;task.spawn(function()while getgenv().rgbCasa do b1("1Player1sHous1e",{"ColorPickHouse",a_(aZ)})task.wait(0.1)end end)end)local b7=false;g:AddToggle({Name="تغير لون اضاء سياره التحت ريمبو",Description="قد يسبب ارتجاج لم يعد اصدار مميز",Default=false,Callback=function(P)b7=P;if P then task.spawn(function()while b7 and B.Character do local b5=a_(aZ)b1("1Player1sCa1r",{"PickingCarColor",b5})task.wait(0.03)end end)end end})local b8=g:AddToggle({Name="سياره ريمبو ",Description="",Default=false})b8:Callback(function(s)getgenv().rgbBicicleta=s;task.spawn(function()while getgenv().rgbBicicleta do b1("1Player1sCa1r",{"NoMotorColor",a_(aZ)})task.wait(0.1)end end)end)local r=g:AddSection({"ادوات ريمبو"})local b9=false;g:AddToggle({Name="راديو ريمبو",Description="",Default=false,Callback=function(P)b9=P;if P then task.spawn(function()while b9 and B.Character do local b5=a_(aZ)local V,aH=pcall(function()return B:WaitForChild("PlayerGui"):WaitForChild("ToolGui"):WaitForChild("ToolSettings"):WaitForChild("Settings"):WaitForChild("PropsColor"):WaitForChild("SetColor")end)if V and aH then pcall(function()aH:FireServer(b5)end)end;task.wait(0.03)end end)end end})
local a=Tab6:AddToggle({Name="مكبر صوت ريمبو",Description="",Default=false})a:Callback(function(b)getgenv().rgbMegafone=b;task.spawn(function()while getgenv().rgbMegafone do local c=getRainbowColor(rgbSpeed)local d=game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")if d then local e=d:FindFirstChild("ToolGui")if e then local f=e:FindFirstChild("ToolSettings")if f then local g=f:FindFirstChild("Settings"):FindFirstChild("PropsColor")if g and g:FindFirstChild("SetColor")then pcall(function()g.SetColor:FireServer(c)end)end end end end;task.wait(0.1)end end)end)local h=Tab6:AddToggle({Name="مال مكياج ريمبو",Description="",Default=false})h:Callback(function(b)getgenv().rgbRosquinha=b;task.spawn(function()while getgenv().rgbRosquinha do local c=getRainbowColor(rgbSpeed)local d=game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")if d then local e=d:FindFirstChild("ToolGui")if e then local f=e:FindFirstChild("ToolSettings")if f then local g=f:FindFirstChild("Settings"):FindFirstChild("PropsColor")if g and g:FindFirstChild("SetColor")then pcall(function()g.SetColor:FireServer(c)end)end end end end;task.wait(0.1)end end)end)local i=Tab7:AddSection({"كل وضائف السياره"})local j=game:GetService("Players")local k=game:GetService("Workspace")local l=j.LocalPlayer;local m=k.CurrentCamera;local n={}n.Players=j;n.Workspace=k;n.LocalPlayer=l;n.Camera=m;function n:MostrarNotificacao(o)pcall(function()game:GetService("StarterGui"):SetCore("SendNotification",{Title="اشعار",Text=o,Duration=5})end)end;function n:ToggleFallDamage(p)if not self.LocalPlayer.Character or not self.LocalPlayer.Character:FindFirstChild("Humanoid")then return false end;local q=self.LocalPlayer.Character.Humanoid;if p then q:SetStateEnabled(Enum.HumanoidStateType.FallingDown,false)q:SetStateEnabled(Enum.HumanoidStateType.Ragdoll,false)q:SetStateEnabled(Enum.HumanoidStateType.GettingUp,true)q.PlatformStand=false;return true else q:SetStateEnabled(Enum.HumanoidStateType.FallingDown,true)q:SetStateEnabled(Enum.HumanoidStateType.Ragdoll,true)return false end end;function n:TeleportToSeat(r,s)if not self.LocalPlayer.Character or not self.LocalPlayer.Character:FindFirstChild("Humanoid")then self:MostrarNotificacao("الشخصية غير موجودة!")return false end;local q=self.LocalPlayer.Character.Humanoid;local t=self.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")if not t then self:MostrarNotificacao("لم يتم العثور على الجذر!")return false end;q.Sit=false;task.wait(0.1)t.CFrame=r.CFrame+Vector3.new(0,5,0)task.wait(0.1)r:Sit(q)task.wait(0.5)return q.SeatPart==r end;function n:TeleportToVoid(s)if not s then self:MostrarNotificacao("سيارة غير صالحة!")return end;if not s.PrimaryPart then local u=s:FindFirstChild("Body",true)or s:FindFirstChild("Chassis",true)if u and u:IsA("BasePart")then s.PrimaryPart=u else self:MostrarNotificacao("لم يتم العثور على الجزء الرئيسي!")return end end;s:SetPrimaryPartCFrame(CFrame.new(0,-1000,0))task.wait(0.5)end;function n:TeleportToPlayer(s,v)if not s then self:MostrarNotificacao("سيارة غير صالحة!")return end;if not s.PrimaryPart then local u=s:FindFirstChild("Body",true)or s:FindFirstChild("Chassis",true)if u and u:IsA("BasePart")then s.PrimaryPart=u else self:MostrarNotificacao("لم يتم العثور على الجزء الرئيسي!")return end end;local w=v+Vector3.new(5,0,5)s:SetPrimaryPartCFrame(CFrame.new(w))task.wait(0.5)end;function n:ExitCarAndReturn(x)if not self.LocalPlayer.Character or not self.LocalPlayer.Character:FindFirstChild("Humanoid")then return end;local q=self.LocalPlayer.Character.Humanoid;if q.SeatPart then q.Sit=false end;task.wait(0.1)if x then self.LocalPlayer.Character:PivotTo(CFrame.new(x))end end;function n:AtualizarListaCarros()local y=self.Workspace:FindFirstChild("Vehicles")local z={}if y then for A,B in ipairs(y:GetChildren())do if B.Name:match("Car$")then table.insert(z,B.Name)end end end;return z end;Tab7:AddParagraph({Title="حماية السيارة",Content=""})Tab7:AddToggle({Name="نقل جميع السيارات",Default=false,Callback=function(C)local D;local E=C;local F=false;if C then if l.Character and l.Character:FindFirstChild("HumanoidRootPart")then D=l.Character.HumanoidRootPart.Position else n:MostrarNotificacao("الشخصية غير موجودة!")return end;F=n:ToggleFallDamage(true)task.spawn(function()local G=n.Workspace:FindFirstChild("Vehicles")if not G then n:MostrarNotificacao("مجلد السيارات غير موجود!")return end;local H={}for A,s in ipairs(G:GetChildren())do if s.Name:match("Car$")then table.insert(H,s)end end;for A,s in ipairs(H)do if not E then break end;local I=s:FindFirstChildWhichIsA("VehicleSeat",true)if I and I.Occupant==nil then local J=n:TeleportToSeat(I,s)if J then n:TeleportToVoid(s)n:ExitCarAndReturn(D)task.wait(1)end end end;if E then E=false;n:ToggleFallDamage(false)end end)else E=false;n:ToggleFallDamage(false)end end})local i=Tab7:AddSection({"وظائف السيارة"})local K=Tab7:AddDropdown({Name="اختر سيارة لاعب",Default=nil,Options=n:AtualizarListaCarros(),Callback=function(L)_G.SelectedVehicle=L end})Tab7:AddToggle({Name="مشاهدة السيارة المحددة",Default=false,Callback=function(C)if C then if not _G.SelectedVehicle or _G.SelectedVehicle==""then n:MostrarNotificacao("لم يتم اختيار سيارة!")return end;local G=k:FindFirstChild("Vehicles")local M=G and G:FindFirstChild(_G.SelectedVehicle)if not M then n:MostrarNotificacao("السيارة غير موجودة!")return end;local I=M:FindFirstChildWhichIsA("VehicleSeat",true)if not I then n:MostrarNotificacao("مقعد السيارة غير موجود!")return end;n.OriginalCameraSubject=m.CameraSubject;n.OriginalCameraType=m.CameraType;m.CameraSubject=I;m.CameraType=Enum.CameraType.Follow;n:MostrarNotificacao("تم ربط الكاميرا بالسيارة!")else if n.OriginalCameraSubject then m.CameraSubject=n.OriginalCameraSubject;m.CameraType=n.OriginalCameraType or Enum.CameraType.Custom;n:MostrarNotificacao("تمت استعادة الكاميرا للوضع الطبيعي!")n.OriginalCameraSubject=nil;n.OriginalCameraType=nil end end end})n.Workspace:WaitForChild("Vehicles").ChildAdded:Connect(function()K:Set(n:AtualizarListaCarros())end)n.Workspace:WaitForChild("Vehicles").ChildRemoved:Connect(function()K:Set(n:AtualizarListaCarros())end)local i=Tab7:AddSection({"تخريب السيارات"})Tab7:AddButton({Name="نقل السيارة المحددة إلى الفراغ",Callback=function()if not _G.SelectedVehicle or _G.SelectedVehicle==""then n:MostrarNotificacao("لم يتم اختيار سيارة!")return end;local G=k:FindFirstChild("Vehicles")local M=G and G:FindFirstChild(_G.SelectedVehicle)if not M then n:MostrarNotificacao("السيارة غير موجودة!")return end;local I=M:FindFirstChildWhichIsA("VehicleSeat",true)if I and not I.Occupant then local x=l.Character and l.Character:FindFirstChild("HumanoidRootPart")and l.Character.HumanoidRootPart.Position;if x then local J=n:TeleportToSeat(I,M)if J then n:TeleportToVoid(M)n:ExitCarAndReturn(x)n:MostrarNotificacao("تم نقل السيارة إلى الفراغ!")end end else n:MostrarNotificacao("هناك شخص في السيارة!")end end})Tab7:AddButton({Name="نقل السيارة إليك",Callback=function()if not _G.SelectedVehicle or _G.SelectedVehicle==""then n:MostrarNotificacao("لم يتم اختيار سيارة!")return end;local G=k:FindFirstChild("Vehicles")local M=G and G:FindFirstChild(_G.SelectedVehicle)if not M then n:MostrarNotificacao("السيارة غير موجودة!")return end;local I=M:FindFirstChildWhichIsA("VehicleSeat",true)if I and not I.Occupant then local x=l.Character and l.Character:FindFirstChild("HumanoidRootPart")and l.Character.HumanoidRootPart.Position;if x then local J=n:TeleportToSeat(I,M)if J then n:TeleportToPlayer(M,x)n:ExitCarAndReturn(x)n:MostrarNotificacao("تم نقل السيارة إليك!")end end else n:MostrarNotificacao("هناك شخص في السيارة!")end end})Tab7:AddButton({Name="تجميع جميع سيارات السيرفر",Callback=function()local x=l.Character and l.Character:FindFirstChild("HumanoidRootPart")and l.Character.HumanoidRootPart.Position;if not x then n:MostrarNotificacao("الشخصية غير موجودة!")return end;local G=k:FindFirstChild("Vehicles")if not G then n:MostrarNotificacao("مجلد السيارات غير موجود!")return end;local H={}for A,s in ipairs(G:GetChildren())do if s.Name:match("Car$")then table.insert(H,s)end end;for A,s in ipairs(H)do local I=s:FindFirstChildWhichIsA("VehicleSeat",true)if I and I.Occupant==nil then local J=n:TeleportToSeat(I,s)if J then n:TeleportToPlayer(s,x)n:ExitCarAndReturn(x)task.wait(1)end end end;if#H==0 then n:MostrarNotificacao("لا توجد سيارات!")end end})local N={"SchoolBus","SmartCar","FarmTruck","Cadillac","Excavator","Jeep","NascarTruck","TowTruck","Snowplow","MilitaryTruck","Tank","Limo","FireTruck"}local O=false;local function P(Q)local R={[1]="PickingCar",[2]=Q}game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(R))end;Tab7:AddToggle({Name="سبام سيارات",Default=false,Callback=function(C)O=C;if O then task.spawn(function()while O do for A,Q in ipairs(N)do if not O then break end;P(Q)task.wait(0)end end end)end end})local j=game:GetService("Players")local S=game:GetService("ReplicatedStorage")local T=j.LocalPlayer;local U=S:WaitForChild("RE")local V=U:FindFirstChild("1Clea1rTool1s")local W=U:FindFirstChild("1Too1l")local X=U:FindFirstChild("1Gu1n")local Y=false;local function Z()if V then V:FireServer("ClearAllTools")end end;local function _()if W then W:InvokeServer("PickingTools","Assault")end end;local function a0()return T.Backpack:FindFirstChild("Assault")~=nil end;local function a1(a2)local a3=T.Backpack:FindFirstChild("Assault")and T.Backpack.Assault:FindFirstChild("GunScript_Local")if not a3 or not a2 then return end;local R={a2,a2,Vector3.new(1e14,1e14,1e14),a2.Position,a3:FindFirstChild("MuzzleEffect"),a3:FindFirstChild("HitEffect"),0,0,{false},{25,Vector3.new(100,100,100),BrickColor.new(29),0.25,Enum.Material.SmoothPlastic,0.25},true,false}X:FireServer(unpack(R))end;local function a4()task.spawn(function()while Y do Z()_()repeat task.wait(0.2)until a0()or not Y;for a5=1,3 do for A,a6 in ipairs(j:GetPlayers())do if not Y then return end;if a6~=T and a6.Character and a6.Character:FindFirstChild("HumanoidRootPart")then a1(a6.Character.HumanoidRootPart)task.wait(0.1)end end end;task.wait(1)end end)end;Tab8:AddToggle({Name="تجميد كل اللاعبين",Description="دون اي نطاق او مجال",Default=false,Callback=function(C)Y=C;if Y then a4()end end})Tab8:AddSection({"تجميد فردي"})Tab8:AddButton({Name="تجميد فردي",Description="بمجرد ضغط الزر ستضهر واجه خاصه ب تجميد فردي",Callback=function()loadstring(game:HttpGet("https://raw.githubusercontent.com/itavvietivvoyd-ux/-/refs/heads/main/%D8%AA%D8%AC%D9%85%D9%8A%D8%AF%20%D9%81%D8%B1%D8%AF%D9%8A%20%D9%85%D8%B9%20%D9%88%D8%A7%D8%AC%D9%87.txt"))()end})
Tab8:AddSection({"تجميد سيرفر مع تاثيرات"})Tab8:AddToggle({Name="توسيع المجال الأخضر",Description="تفعيل وضع المجال الأخضر المضيء والهجوم",Default=false,Callback=function(a)if a then local b=game:GetService("Lighting")local c=game:GetService("Players")local d=game:GetService("ReplicatedStorage")local e=c.LocalPlayer;local function f()local g=e.Character or e.CharacterAdded:Wait()local h=g:WaitForChild("HumanoidRootPart")local i=Instance.new("Model",workspace)i.Name="InfiniteVoid"local j=Instance.new("Part")j.Shape=Enum.PartType.Ball;j.Size=Vector3.new(300,300,300)j.Position=h.Position;j.Anchored=true;j.CanCollide=false;j.Material=Enum.Material.ForceField;j.Transparency=0.3;j.Color=Color3.fromRGB(0,255,0)j.Parent=i;local k=Instance.new("PointLight",j)k.Color=Color3.fromRGB(0,255,0)k.Brightness=10;k.Range=300;local l=Instance.new("ParticleEmitter",j)l.Texture="rbxassetid://243660364"l.Color=ColorSequence.new(Color3.fromRGB(0,255,0))l.LightEmission=1;l.Size=NumberSequence.new(3)l.Transparency=NumberSequence.new(0.2)l.Rate=1000;l.Lifetime=NumberRange.new(2)l.Speed=NumberRange.new(0)l.VelocitySpread=180;local m=Instance.new("Sound",j)m.SoundId="rbxassetid://1843527678"m.Volume=2;m.Looped=true;m:Play()local n=b:FindFirstChildOfClass("Sky")if n then n:Destroy()end;local o=Instance.new("Sky",b)o.SkyboxBk="rbxassetid://159454299"o.SkyboxDn="rbxassetid://159454296"o.SkyboxFt="rbxassetid://159454293"o.SkyboxLf="rbxassetid://159454286"o.SkyboxRt="rbxassetid://159454300"o.SkyboxUp="rbxassetid://159454288"end;f()local p=140031333626044;local q=true;task.spawn(function()while q and a do local r=d:FindFirstChild("RE")and d.RE:FindFirstChild("1Gu1nSound1s")if r then r:FireServer(workspace,p,1)end;local s=e.Character and e.Character:FindFirstChild("HumanoidRootPart")if s then local t=Instance.new("Sound")t.SoundId="rbxassetid://"..p;t.Volume=1;t.Looped=false;t.Parent=s;t:Play()t.Ended:Connect(function()t:Destroy()end)task.wait(t.TimeLength+0.1)else break end end end)else if workspace:FindFirstChild("InfiniteVoid")then workspace.InfiniteVoid:Destroy()end;local b=game:GetService("Lighting")local u=b:FindFirstChildOfClass("Sky")if u then u:Destroy()end end end})Tab8:AddToggle({Name="توسيع المجال بنفسجي",Description="تأثير فضائي جميل",Default=false,Callback=function(a)local b=game:GetService("Lighting")local c=game:GetService("Players")local e=c.LocalPlayer;if not b:FindFirstChild("OldSkyBackup")then local u=b:FindFirstChildOfClass("Sky")if u then u.Name="OldSkyBackup"end end;if a then local g=e.Character or e.CharacterAdded:Wait()local h=g:WaitForChild("HumanoidRootPart")local v=Instance.new("Model",workspace)v.Name="InfiniteVoid_Violet"local j=Instance.new("Part")j.Shape=Enum.PartType.Ball;j.Size=Vector3.new(350,350,350)j.Position=h.Position;j.Anchored=true;j.CanCollide=false;j.Material=Enum.Material.ForceField;j.Transparency=0.2;j.Color=Color3.fromRGB(160,0,255)j.Parent=v;local w=Instance.new("PointLight",j)w.Color=Color3.fromRGB(160,0,255)w.Brightness=12;w.Range=350;local x=Instance.new("ParticleEmitter",j)x.Texture="rbxassetid://243660364"x.Color=ColorSequence.new(Color3.fromRGB(160,0,255))x.LightEmission=1;x.Size=NumberSequence.new(4)x.Rate=900;x.Lifetime=NumberRange.new(3)x.Speed=NumberRange.new(0)x.Transparency=NumberSequence.new(0.15)local y=b:FindFirstChild("DomainSky")if y then y:Destroy()end;local o=Instance.new("Sky")o.Name="DomainSky"o.SkyboxBk="rbxassetid://159454299"o.SkyboxDn="rbxassetid://159454296"o.SkyboxFt="rbxassetid://159454293"o.SkyboxLf="rbxassetid://159454286"o.SkyboxRt="rbxassetid://159454300"o.SkyboxUp="rbxassetid://159454288"o.Parent=b else if workspace:FindFirstChild("InfiniteVoid_Violet")then workspace.InfiniteVoid_Violet:Destroy()end;local z=b:FindFirstChild("DomainSky")if z then z:Destroy()end;local u=b:FindFirstChild("OldSkyBackup")if u then u.Name="Sky"else Instance.new("Sky",b).Name="Sky"end end end})local c=game:GetService("Players")local A=game:GetService("Workspace")local B=game:GetService("RunService")local d=game:GetService("ReplicatedStorage")local C=c.LocalPlayer;local D=nil;local E=false;local F=false;local G=false;local H=nil;local I=nil;local J=nil;local K=nil;local L={}local M=nil;local N=nil;local O=nil;local P=nil;local Q=false;local R=nil;local S=nil;local T=nil;local U=Instance.new("RemoteEvent")U.Name="SetNetworkOwnerEvent_"..tostring(math.random(1000,9999))U.Parent=d;local V=[[local ReplicatedStorage=game:GetService("ReplicatedStorage")local event=ReplicatedStorage:WaitForChild("]]..U.Name..[[")event.OnServerEvent:Connect(function(player,part,networkOwner)if part and part:IsA("BasePart")then pcall(function()part:SetNetworkOwner(networkOwner)part.Anchored=false part.CanCollide=true part.CanTouch=true end)end end)]]pcall(function()loadstring(V)()end)local function W()local X=C:WaitForChild("Backpack")local Y=X:FindFirstChild("CarClient")if Y and Y:IsA("LocalScript")then Y.Disabled=true end end;local function Z()local X=C:WaitForChild("Backpack")local Y=X:FindFirstChild("CarClient")if Y and Y:IsA("LocalScript")then Y.Disabled=false end end;local function _()local a0={}for a1,a2 in ipairs(c:GetPlayers())do if a2~=C then table.insert(a0,a2.Name)end end;return a0 end;local function a3(a4,a5)pcall(function()local a6=a4:Get()local a0=_()a4:Set(a0)if a6 and not table.find(a0,a6)then a4:Set("")D=nil;if Q then stopSpectating()if a5 then pcall(function()a5:Set(false)end)end end;if G or E or F then G=false;E=false;F=false;if H then H:Disconnect()H=nil end;if I then I:Disconnect()I=nil end;if T then pcall(function()T:Set(false)end)end end elseif a6 and table.find(a0,a6)then a4:Set(a6)end end)end;local function a7(a8)if S then S:Disconnect()S=nil end;local a9=c:FindFirstChild(a8)if a9 and a9~=C then R=a9;Q=true;local function aa()if not Q or not R then return end;if R.Character and R.Character:FindFirstChild("Humanoid")then A.CurrentCamera.CameraSubject=R.Character.Humanoid else A.CurrentCamera.CameraSubject=nil end end;aa()S=B.Heartbeat:Connect(function()if not Q then S:Disconnect()S=nil;return end;pcall(aa)end)R.CharacterAdded:Connect(function()if Q then aa()end end)else Q=false;R=nil end end;local function stopSpectating()if S then S:Disconnect()S=nil end;Q=false;R=nil;if C.Character and C.Character:FindFirstChild("Humanoid")then A.CurrentCamera.CameraSubject=C.Character.Humanoid;A.CurrentCamera.CameraType=Enum.CameraType.Custom else A.CurrentCamera.CameraSubject=nil;A.CurrentCamera.CameraType=Enum.CameraType.Custom end end;local function ab(a8)local a9=c:FindFirstChild(a8)if a9 and C.Character and C.Character:FindFirstChild("HumanoidRootPart")and a9.Character and a9.Character:FindFirstChild("HumanoidRootPart")then local ac=C.Character:FindFirstChild("HumanoidRootPart")local ad=C.Character:FindFirstChild("Humanoid")if not ac or not ad then print("Seu personagem não está totalmente carregado para teletransporte.")return end;for a1,ae in ipairs(C.Character:GetDescendants())do if ae:IsA("BasePart")then ae.Velocity=Vector3.zero;ae.RotVelocity=Vector3.zero;ae.Anchored=true end end;local af,ag=pcall(function()ac.CFrame=CFrame.new(a9.Character.HumanoidRootPart.Position+Vector3.new(0,2,0))end)if not af then warn("Erro ao teletransportar:"..tostring(ag))return end;ad.Sit=false;ad:ChangeState(Enum.HumanoidStateType.GettingUp)task.wait(0.5)for a1,ae in ipairs(C.Character:GetDescendants())do if ae:IsA("BasePart")then ae.Anchored=false;ae.Velocity=Vector3.zero;ae.RotVelocity=Vector3.zero end end;print("Teletransportado para o jogador:"..a8 .." com ancoragem segura.")else print("Jogador ou personagem não encontrado para teletransporte.")end end;C.CharacterAdded:Connect(function(ah)if Q then stopSpectating()pcall(function()SpectateToggleTab10:Set(false)end)end end)local ai;local aj=Tab9:AddDropdown({Name="اختر لاعب",Description="",Default="",Multi=false,Options=_(),Flag="player list",Callback=function(ak)ai=ak;if ak==""or ak==nil then D=nil;if G or E or F then G=false;E=false;F=false;if H then H:Disconnect()end;if I then I:Disconnect()end;if T then pcall(function()T:Set(false)end)end end;if Q then stopSpectating()end else D=c:FindFirstChild(ak)if Q then stopSpectating()a7(ak)end end end})function UptadePlayers()local a0={}for a1,a2 in ipairs(c:GetPlayers())do if a2.Name~=C.Name then table.insert(a0,a2.Name)end end;aj:Set(a0)end;Tab9:AddButton({"تحديث قائمه",function()UptadePlayers()end})UptadePlayers()Tab9:AddButton({Title="انتقل للاعب",Desc="",Callback=function()local ak=ai;if ak and ak~=""then local af,ag=pcall(ab,ak)if not af then warn("Erro ao teletransportar:"..tostring(ag))end else print("Selecione um jogador antes de teletransportar.")end end})local SpectateToggleTab10=Tab9:AddToggle({Name="مشاهده لاعب",Description="",Default=false,Callback=function(a)if a then if D then pcall(a7,D.Name)else SpectateToggleTab10:Set(false)end else pcall(stopSpectating)end end})
Players.PlayerRemoving:Connect(function(a)updateDropdown(DropdownPlayerTab2,SpectateToggleTab10)if selectedPlayer==a then selectedPlayer=nil;if isSpectating then stopSpectating()end;if running then running=false;if connection then connection:Disconnect()connection=nil end;if flingConnection then flingConnection:Disconnect()flingConnection=nil end;if flingToggle then flingToggle:Set(false)end end;SpectateToggleTab10:Set(false)DropdownPlayerTab2:Set("")end end)Players.PlayerAdded:Connect(function()task.wait(1)updateDropdown(DropdownPlayerTab2,SpectateToggleTab10)end)updateDropdown(DropdownPlayerTab2,SpectateToggleTab10)local b=Tab9:AddSection({"القتل"})local c=Tab9:AddDropdown({Name="اختر طريقه قتل",Description="",Options={"كنبه","باص"},Callback=function(d)selectedKillPullMethod=d end})local function e()local f=LocalPlayer:WaitForChild("Backpack")local g=f:FindFirstChild("Couch")or LocalPlayer.Character:FindFirstChild("Couch")if not g then local h={[1]="PickingTools",[2]="Couch"}local i=pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer(unpack(h))end)if not i then return false end;repeat g=f:FindFirstChild("Couch")task.wait()until g or task.wait(5)if not g then return false end end;if g.Parent~=LocalPlayer.Character then g.Parent=LocalPlayer.Character end;return true end;local function j(k)if not k or not k.Character or not LocalPlayer.Character then return end;if not e()then return end;isFollowingKill=true;originalPosition=LocalPlayer.Character:FindFirstChild("HumanoidRootPart").Position end;local function l(k)if not k or not k.Character or not LocalPlayer.Character then return end;if not e()then return end;isFollowingPull=true;originalPosition=LocalPlayer.Character:FindFirstChild("HumanoidRootPart").Position end;local function m(k)if not k or not k.Character or not LocalPlayer.Character then return end;local n=LocalPlayer.Character;local o=n:FindFirstChildOfClass("Humanoid")local p=n:FindFirstChild("HumanoidRootPart")if not o or not p then return end;savedPosition=p.Position;pcall(function()p.Anchored=true;p.CFrame=CFrame.new(Vector3.new(1181.83,76.08,-1158.83))task.wait(0.2)p.Velocity=Vector3.zero;p.RotVelocity=Vector3.zero;p.Anchored=false;if o then o:ChangeState(Enum.HumanoidStateType.GettingUp)end end)task.wait(0.5)disableCarClient()local h={[1]="DeleteAllVehicles"}pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(h))end)h={[1]="PickingCar",[2]="SchoolBus"}pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(h))end)task.wait(1)local q=Workspace:FindFirstChild("Vehicles")if not q then return end;local r=LocalPlayer.Name.."Car"local s=q:FindFirstChild(r)if not s then return end;pcall(function()p.Anchored=true;p.CFrame=CFrame.new(Vector3.new(1171.15,79.45,-1166.2))task.wait(0.2)p.Velocity=Vector3.zero;p.RotVelocity=Vector3.zero;p.Anchored=false;o:ChangeState(Enum.HumanoidStateType.Seated)end)local t=tick()repeat task.wait()if tick()-t>10 then return end until o.Sit;for u,v in ipairs(s:GetDescendants())do if v:IsA("BasePart")then v.CanCollide=false;pcall(function()v:SetNetworkOwner(nil)end)end end;running=true;connection=RunService.Stepped:Connect(function()if not running then return end;for u,v in ipairs(n:GetDescendants())do if v:IsA("BasePart")then v.CanCollide=false end end end)local w=tick()local x=0.05;local y=tick()flingConnection=RunService.Heartbeat:Connect(function()if not running then return end;local z=k.Character or k.CharacterAdded:Wait()local A=z:FindFirstChild("HumanoidRootPart")local B=z:FindFirstChild("Humanoid")if not A or not B then return end;if not p or not o then running=false;return end;if tick()-w<x then return end;w=tick()local C=Vector3.new(math.random(-10,10),0,math.random(-10,10))pcall(function()local D=A.Position+C;s:PivotTo(CFrame.new(D)*CFrame.Angles(math.rad(Workspace.DistributedGameTime*12000),math.rad(Workspace.DistributedGameTime*15000),math.rad(Workspace.DistributedGameTime*18000)))end)local E=false;for u,F in ipairs(s:GetDescendants())do if(F:IsA("Seat")or F:IsA("VehicleSeat"))and F.Name~="VehicleSeat"then if F.Occupant==B then E=true;break end end end;if E or tick()-y>10 then running=false;if connection then connection:Disconnect()connection=nil end;if flingConnection then flingConnection:Disconnect()flingConnection=nil end;pcall(function()s:PivotTo(CFrame.new(Vector3.new(-76.6,-401.97,-84.26)))end)task.wait(0.5)disableCarClient()local h={[1]="DeleteAllVehicles"}pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(h))end)if n then local p=n:FindFirstChild("HumanoidRootPart")if p and savedPosition then pcall(function()p.Anchored=true;p.CFrame=CFrame.new(savedPosition+Vector3.new(0,5,0))task.wait(0.2)p.Velocity=Vector3.zero;p.RotVelocity=Vector3.zero;p.Anchored=false;if o then o:ChangeState(Enum.HumanoidStateType.GettingUp)end end)end end;if n then for u,v in ipairs(n:GetDescendants())do if v:IsA("BasePart")then v.CanCollide=true;v.Velocity=Vector3.zero;v.RotVelocity=Vector3.zero end end end;local G=n and n:FindFirstChild("Humanoid")if G then G:SetStateEnabled(Enum.HumanoidStateType.Seated,true)end;for u,F in ipairs(Workspace:GetDescendants())do if F:IsA("Seat")or F:IsA("VehicleSeat")then F.Disabled=false end end;pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clothe1s"):FireServer("CharacterSizeUp",1)end)end end)end;local H;if H then H:Disconnect()end;H=RunService.Heartbeat:Connect(function()if(isFollowingKill or isFollowingPull)and selectedPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")and selectedPlayer.Character and selectedPlayer.Character:FindFirstChild("HumanoidRootPart")then pcall(function()local D=selectedPlayer.Character.HumanoidRootPart.Position;LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(D)*CFrame.Angles(math.rad(Workspace.DistributedGameTime*12000),math.rad(Workspace.DistributedGameTime*15000),math.rad(Workspace.DistributedGameTime*18000)))end)end end)local I;if I then I:Disconnect()end;I=RunService.Heartbeat:Connect(function()if(isFollowingKill or isFollowingPull)and selectedPlayer and selectedPlayer.Character and selectedPlayer.Character:FindFirstChild("Humanoid")then pcall(function()if selectedPlayer.Character.Humanoid.Sit then if isFollowingKill then if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")then LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(0,-500,0))task.wait(0.5)ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools","Couch")task.wait(1)end end;isFollowingKill=false;isFollowingPull=false;if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")and originalPosition then local p=LocalPlayer.Character:FindFirstChild("HumanoidRootPart")local G=LocalPlayer.Character:FindFirstChild("Humanoid")if p then p.Anchored=true;p.CFrame=CFrame.new(originalPosition+Vector3.new(0,5,0))task.wait(0.2)p.Velocity=Vector3.zero;p.RotVelocity=Vector3.zero;p.Anchored=false;if G then G:ChangeState(Enum.HumanoidStateType.GettingUp)end end;originalPosition=nil end end end)end end)Tab9:AddButton({Name="قتل",Description="",Callback=function()if isFollowingKill or isFollowingPull or running then return end;if not selectedPlayer or not selectedKillPullMethod then return end;if selectedKillPullMethod=="Sofá"then j(selectedPlayer)elseif selectedKillPullMethod=="Ônibus"then m(selectedPlayer)end end})Tab9:AddButton({Name="سحب",Description="",Callback=function()if isFollowingKill or isFollowingPull or running then return end;if not selectedPlayer or not selectedKillPullMethod or selectedKillPullMethod~="Sofá"then return end;l(selectedPlayer)end})Tab9:AddButton({Name="ايقاف القتل والسحب",Description="",Callback=function()isFollowingKill=false;isFollowingPull=false;for u,v in ipairs(LocalPlayer.Character:GetDescendants())do if v:IsA("BasePart")then v.CanCollide=true;v.Velocity=Vector3.zero;v.RotVelocity=Vector3.zero end end;local G=LocalPlayer.Character:FindFirstChild("Humanoid")if G then G:SetStateEnabled(Enum.HumanoidStateType.Seated,true)end;for u,F in ipairs(Workspace:GetDescendants())do if F:IsA("Seat")or F:IsA("VehicleSeat")then F.Disabled=false end end;if originalPosition then local p=LocalPlayer.Character:FindFirstChild("HumanoidRootPart")if p then p.Anchored=true;p.CFrame=CFrame.new(originalPosition+Vector3.new(0,5,0))task.wait(0.2)p.Velocity=Vector3.zero;p.RotVelocity=Vector3.zero;p.Anchored=false;if G then G:ChangeState(Enum.HumanoidStateType.GettingUp)end end;originalPosition=nil end;disableCarClient()local h={[1]="DeleteAllVehicles"}pcall(function()ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(h))end)end})local b=Tab9:AddSection({"فلنقات نادره"})local J=Tab9:AddDropdown({Name="انواع فلنقات",Description="",Options={"كنبه","باص","كره","كره V2 ","سفينه","تريله"},Callback=function(d)selectedFlingMethod=d end})
local function flingWithSofa(targetPlayer)
    if not targetPlayer or not targetPlayer.Character or not LocalPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not myHRP then
        return
    end
    savedPosition = myHRP.Position
    if not equipSofa() then
        return
    end
    task.wait(0.5)
    couch = character:FindFirstChild("Couch")
    if not couch then
        for _, obj in ipairs(Workspace:GetDescendants()) do
            if
                (obj.Name == "Couch" or obj.Name == "Couch" .. LocalPlayer.Name) and
                    (obj:IsA("BasePart") or obj:IsA("Tool"))
             then
                couch = obj
                break
            end
        end
    end
    if not couch then
        return
    end
    if couch:IsA("BasePart") then
        originalProperties = {Anchored = couch.Anchored, CanCollide = couch.CanCollide, CanTouch = couch.CanTouch}
        couch.Anchored = false
        couch.CanCollide = true
        couch.CanTouch = true
        pcall(
            function()
                couch:SetNetworkOwner(nil)
            end
        )
    end
    running = true
    connection =
        RunService.Stepped:Connect(
        function()
            if not running then
                return
            end
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    )
    local startTime = tick()
    local walkFlingInstance = nil
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running then
                return
            end
            if not targetPlayer or not targetPlayer.Character then
                running = false
                return
            end
            local newTargetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local newTargetHumanoid = targetPlayer.Character:FindFirstChild("Humanoid")
            if not newTargetHRP or not newTargetHumanoid then
                running = false
                return
            end
            if not myHRP or not humanoid then
                running = false
                return
            end
            pcall(
                function()
                    local targetPosition = newTargetHRP.Position
                    character:SetPrimaryPartCFrame(
                        CFrame.new(targetPosition) *
                            CFrame.Angles(
                                math.rad(Workspace.DistributedGameTime * 12000),
                                math.rad(Workspace.DistributedGameTime * 15000),
                                math.rad(Workspace.DistributedGameTime * 18000)
                            )
                    )
                end
            )
            if newTargetHumanoid.Sit or tick() - startTime > 10 then
                running = false
                flingConnection:Disconnect()
                flingConnection = nil
                for _, part in ipairs(character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                        pcall(
                            function()
                                part:SetNetworkOwner(nil)
                            end
                        )
                    end
                end
                walkFlingInstance = Instance.new("BodyVelocity")
                walkFlingInstance.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                walkFlingInstance.Velocity =
                    Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 1000000 + Vector3.new(0, 1000000, 0)
                walkFlingInstance.Parent = myHRP
                pcall(
                    function()
                        myHRP.Anchored = true
                        myHRP.CFrame = CFrame.new(Vector3.new(-59599.73, 2040070.50, -293391.16))
                        myHRP.Anchored = false
                    end
                )
                local spinStart = tick()
                local spinConnection
                spinConnection =
                    RunService.Heartbeat:Connect(
                    function()
                        if tick() - spinStart >= 0.5 then
                            spinConnection:Disconnect()
                            return
                        end
                        pcall(
                            function()
                                character:SetPrimaryPartCFrame(
                                    myHRP.CFrame *
                                        CFrame.Angles(
                                            math.rad(Workspace.DistributedGameTime * 12000),
                                            math.rad(Workspace.DistributedGameTime * 15000),
                                            math.rad(Workspace.DistributedGameTime * 18000)
                                        )
                                )
                            end
                        )
                    end
                )
                task.wait(0.5)
                local args = {[1] = "PlayerWantsToDeleteTool", [2] = "Couch"}
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))
                    end
                )
                pcall(
                    function()
                        myHRP.Anchored = true
                        myHRP.CFrame = CFrame.new(savedPosition + Vector3.new(0, 5, 0))
                        task.wait(0.2)
                        myHRP.Velocity = Vector3.zero
                        myHRP.RotVelocity = Vector3.zero
                        myHRP.Anchored = false
                        if humanoid then
                            humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                        end
                    end
                )
                if walkFlingInstance then
                    walkFlingInstance:Destroy()
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = true
                        end
                    end
                end
                if flingToggle then
                    flingToggle:SetValue(false)
                end
            end
        end
    )
end
local function equipBola()
    local backpack = LocalPlayer:WaitForChild("Backpack")
    local bola = backpack:FindFirstChild("SoccerBall") or LocalPlayer.Character:FindFirstChild("SoccerBall")
    if not bola then
        local args = {[1] = "PickingTools", [2] = "SoccerBall"}
        local success =
            pcall(
            function()
                ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer(unpack(args))
            end
        )
        if not success then
            return false
        end
        repeat
            bola = backpack:FindFirstChild("SoccerBall")
            task.wait()
        until bola or task.wait(5)
        if not bola then
            return false
        end
    end
    if bola.Parent ~= LocalPlayer.Character then
        bola.Parent = LocalPlayer.Character
    end
    return true
end
local function flingWithBall(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not myHRP then
        return
    end
    if not equipBola() then
        return
    end
    task.wait(0.5)
    local args = {[1] = "PlayerWantsToDeleteTool", [2] = "SoccerBall"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))
        end
    )
    local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
    if not workspaceCom then
        return
    end
    local soccerBalls = workspaceCom:FindFirstChild("001_SoccerBalls")
    if not soccerBalls then
        return
    end
    soccerBall = soccerBalls:FindFirstChild("Soccer" .. LocalPlayer.Name)
    if not soccerBall then
        return
    end
    originalProperties = {
        Anchored = soccerBall.Anchored,
        CanCollide = soccerBall.CanCollide,
        CanTouch = soccerBall.CanTouch
    }
    soccerBall.Anchored = false
    soccerBall.CanCollide = true
    soccerBall.CanTouch = true
    pcall(
        function()
            soccerBall:SetNetworkOwner(nil)
        end
    )
    savedPosition = myHRP.Position
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
        end
    end
    if humanoid then
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
        humanoid.Sit = false
    end
    for _, seat in ipairs(Workspace:GetDescendants()) do
        if seat:IsA("Seat") or seat:IsA("VehicleSeat") then
            seat.Disabled = true
        end
    end
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clothe1s"):FireServer("CharacterSizeDown", 4)
        end
    )
    running = true
    local lastFlingTime = 0
    connection =
        RunService.Heartbeat:Connect(
        function()
            if not running or not targetPlayer.Character then
                return
            end
            local hrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local hum = targetPlayer.Character:FindFirstChild("Humanoid")
            local myHRP = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if not hrp or not hum or not myHRP then
                return
            end
            local moveDir = hum.MoveDirection
            local isStill = moveDir.Magnitude < 0.1
            local isSitting = hum.Sit
            if isSitting then
                local y = math.sin(tick() * 50) * 2
                soccerBall.CFrame = CFrame.new(hrp.Position + Vector3.new(0, 0.75 + y, 0))
            elseif isStill then
                local z = math.sin(tick() * 50) * 3
                soccerBall.CFrame = CFrame.new(hrp.Position + Vector3.new(0, 0.75, z))
            else
                local offset = moveDir.Unit * math.clamp(hrp.Velocity.Magnitude * 0.15, 5, 12)
                soccerBall.CFrame = CFrame.new(hrp.Position + offset + Vector3.new(0, 0.75, 0))
            end
            myHRP.CFrame = CFrame.new(soccerBall.Position + Vector3.new(0, 1, 0))
        end
    )
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running or not targetPlayer.Character then
                return
            end
            local hrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then
                return
            end
            local dist = (soccerBall.Position - hrp.Position).Magnitude
            if dist < 4 and tick() - lastFlingTime > 0.4 then
                lastFlingTime = tick()
                for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
                local fling = Instance.new("BodyVelocity")
                fling.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                fling.Velocity =
                    Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 500000 + Vector3.new(0, 250000, 0)
                fling.Parent = hrp
                task.delay(
                    0.3,
                    function()
                        fling:Destroy()
                        for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = true
                            end
                        end
                    end
                )
            end
        end
    )
end
local function flingWithBallV2(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not myHRP then
        return
    end
    if not equipBola() then
        return
    end
    task.wait(0.5)
    local args = {[1] = "PlayerWantsToDeleteTool", [2] = "SoccerBall"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))
        end
    )
    local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
    if not workspaceCom then
        return
    end
    local soccerBalls = workspaceCom:FindFirstChild("001_SoccerBalls")
    if not soccerBalls then
        return
    end
    soccerBall = soccerBalls:FindFirstChild("Soccer" .. LocalPlayer.Name)
    if not soccerBall then
        return
    end
    originalProperties = {
        Anchored = soccerBall.Anchored,
        CanCollide = soccerBall.CanCollide,
        CanTouch = soccerBall.CanTouch
    }
    soccerBall.Anchored = false
    soccerBall.CanCollide = true
    soccerBall.CanTouch = true
    pcall(
        function()
            soccerBall:SetNetworkOwner(nil)
        end
    )
    savedPosition = myHRP.Position
    running = true
    local lastFlingTime = 0
    connection =
        RunService.Heartbeat:Connect(
        function()
            if not running or not targetPlayer.Character then
                return
            end
            local hrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local hum = targetPlayer.Character:FindFirstChild("Humanoid")
            if not hrp or not hum then
                return
            end
            local speed = hrp.Velocity.Magnitude
            local isMoving = hum.MoveDirection.Magnitude > 0.05
            local isJumping = hum:GetState() == Enum.HumanoidStateType.Jumping
            local offset
            if isMoving or isJumping then
                local extra = math.clamp(speed / 1.5, 6, 15)
                offset = hrp.CFrame.LookVector * extra + Vector3.new(0, 1, 0)
            else
                local wave = math.sin(tick() * 25) * 4
                local side = math.cos(tick() * 20) * 1.5
                offset = Vector3.new(side, 1, wave)
            end
            pcall(
                function()
                    soccerBall.CFrame = CFrame.new(hrp.Position + offset)
                    soccerBall.AssemblyLinearVelocity = Vector3.new(9999, 9999, 9999)
                end
            )
        end
    )
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running or not targetPlayer.Character then
                return
            end
            local hrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then
                return
            end
            local dist = (soccerBall.Position - hrp.Position).Magnitude
            if dist < 4 and tick() - lastFlingTime > 0.4 then
                lastFlingTime = tick()
                for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
                local fling = Instance.new("BodyVelocity")
                fling.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                fling.Velocity =
                    Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 500000 + Vector3.new(0, 250000, 0)
                fling.Parent = hrp
                task.delay(
                    0.3,
                    function()
                        fling:Destroy()
                        for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = true
                            end
                        end
                    end
                )
            end
        end
    )
end
local function flingWithBus(targetPlayer)
    if not targetPlayer or not targetPlayer.Character or not LocalPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not myHRP then
        return
    end
    savedPosition = myHRP.Position
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(1181.83, 76.08, -1158.83))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    )
    task.wait(0.5)
    disableCarClient()
    local args = {[1] = "DeleteAllVehicles"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    args = {[1] = "PickingCar", [2] = "SchoolBus"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    task.wait(1)
    local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
    if not vehiclesFolder then
        return
    end
    local busName = LocalPlayer.Name .. "Car"
    local bus = vehiclesFolder:FindFirstChild(busName)
    if not bus then
        return
    end
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(1171.15, 79.45, -1166.2))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            humanoid:ChangeState(Enum.HumanoidStateType.Seated)
        end
    )
    local sitStart = tick()
    repeat
        task.wait()
        if tick() - sitStart > 10 then
            return
        end
    until humanoid.Sit
    for _, part in ipairs(bus:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
            pcall(
                function()
                    part:SetNetworkOwner(nil)
                end
            )
        end
    end
    running = true
    connection =
        RunService.Stepped:Connect(
        function()
            if not running then
                return
            end
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    )
    local startTime = tick()
    local walkFlingInstancePlayer = nil
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running then
                return
            end
            if not targetPlayer or not targetPlayer.Character then
                running = false
                return
            end
            local newTargetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local newTargetHumanoid = targetPlayer.Character:FindFirstChild("Humanoid")
            if not newTargetHRP or not newTargetHumanoid then
                running = false
                return
            end
            if not myHRP or not humanoid then
                running = false
                return
            end
            local offset = Vector3.new(math.random(-10, 10), 0, math.random(-10, 10))
            pcall(
                function()
                    local targetPosition = newTargetHRP.Position + offset
                    bus:PivotTo(
                        CFrame.new(targetPosition) *
                            CFrame.Angles(
                                math.rad(Workspace.DistributedGameTime * 12000),
                                math.rad(Workspace.DistributedGameTime * 15000),
                                math.rad(Workspace.DistributedGameTime * 18000)
                            )
                    )
                end
            )
            local playerSeated = false
            for _, seat in ipairs(bus:GetDescendants()) do
                if (seat:IsA("Seat") or seat:IsA("VehicleSeat")) and seat.Name ~= "VehicleSeat" then
                    if seat.Occupant == newTargetHumanoid then
                        playerSeated = true
                        break
                    end
                end
            end
            if playerSeated or tick() - startTime > 10 then
                running = false
                flingConnection:Disconnect()
                flingConnection = nil
                pcall(
                    function()
                        bus:PivotTo(CFrame.new(Vector3.new(-59599.73, 2040070.50, -293391.16)))
                    end
                )
                walkFlingInstancePlayer = Instance.new("BodyVelocity")
                walkFlingInstancePlayer.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                walkFlingInstancePlayer.Velocity =
                    Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 1000000 + Vector3.new(0, 1000000, 0)
                walkFlingInstancePlayer.Parent = myHRP
                task.wait(0.5)
                disableCarClient()
                local args = {[1] = "DeleteAllVehicles"}
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
                    end
                )
                if walkFlingInstancePlayer then
                    walkFlingInstancePlayer:Destroy()
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = true
                        end
                    end
                end
                pcall(
                    function()
                        myHRP.Anchored = true
                        myHRP.CFrame = CFrame.new(savedPosition + Vector3.new(0, 5, 0))
                        task.wait(0.2)
                        myHRP.Velocity = Vector3.zero
                        myHRP.RotVelocity = Vector3.zero
                        myHRP.Anchored = false
                        if humanoid then
                            humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                        end
                    end
                )
                if flingToggle then
                    flingToggle:Set(false)
                end
            end
        end
    )
end
local function flingWithBoat(targetPlayer)
    if not targetPlayer or not targetPlayer.Character or not LocalPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not myHRP then
        return
    end
    savedPosition = myHRP.Position
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(-3359.52, -5.05, -501.94))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    )
    disableCarClient()
    local args = {[1] = "DeleteAllVehicles"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    task.wait(0.4)
    args = {[1] = "PickingBoat", [2] = "MilitaryBoatFree"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    task.wait(1.5)
    local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
    if not vehiclesFolder then
        return
    end
    local boatName = LocalPlayer.Name .. "Car"
    local boat = vehiclesFolder:FindFirstChild(boatName)
    if not boat then
        return
    end
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(-3358.85, 5.25, -521.95))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            humanoid:ChangeState(Enum.HumanoidStateType.Seated)
        end
    )
    local sitStart = tick()
    repeat
        task.wait()
        if tick() - sitStart > 10 then
            return
        end
    until humanoid.Sit
    for _, part in ipairs(boat:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
            pcall(
                function()
                    part:SetNetworkOwner(nil)
                end
            )
        end
    end
    running = true
    connection =
        RunService.Stepped:Connect(
        function()
            if not running then
                return
            end
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    )
    local startTime = tick()
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running then
                return
            end
            if not targetPlayer or not targetPlayer.Character then
                running = false
                return
            end
            local newTargetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local newTargetHumanoid = targetPlayer.Character:FindFirstChild("Humanoid")
            if not newTargetHRP or not newTargetHumanoid then
                running = false
                return
            end
            if not myHRP or not humanoid then
                running = false
                return
            end
            local offset = Vector3.new(math.random(-10, 10), 0, math.random(-10, 10))
            pcall(
                function()
                    local targetPosition = newTargetHRP.Position + offset
                    boat:PivotTo(
                        CFrame.new(targetPosition) *
                            CFrame.Angles(
                                math.rad(Workspace.DistributedGameTime * 12000),
                                math.rad(Workspace.DistributedGameTime * 15000),
                                math.rad(Workspace.DistributedGameTime * 18000)
                            )
                    )
                end
            )
            local playerSeated = false
            for _, seat in ipairs(boat:GetDescendants()) do
                if (seat:IsA("Seat") or seat:IsA("VehicleSeat")) and seat.Name ~= "VehicleSeat" then
                    if seat.Occupant == newTargetHumanoid then
                        playerSeated = true
                        break
                    end
                end
            end
            if playerSeated or tick() - startTime > 10 then
                running = false
                if connection then
                    connection:Disconnect()
                    connection = nil
                end
                if flingConnection then
                    flingConnection:Disconnect()
                    flingConnection = nil
                end
                pcall(
                    function()
                        boat:PivotTo(CFrame.new(Vector3.new(-76.6, -401.97, -84.26)))
                    end
                )
                task.wait(0.5)
                disableCarClient()
                local args = {[1] = "DeleteAllVehicles"}
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
                    end
                )
                if character then
                    local myHRP = character:FindFirstChild("HumanoidRootPart")
                    if myHRP and savedPosition then
                        pcall(
                            function()
                                myHRP.Anchored = true
                                myHRP.CFrame = CFrame.new(savedPosition + Vector3.new(0, 5, 0))
                                task.wait(0.2)
                                myHRP.Velocity = Vector3.zero
                                myHRP.RotVelocity = Vector3.zero
                                myHRP.Anchored = false
                                if humanoid then
                                    humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                                end
                            end
                        )
                    end
                end
                if character then
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = true
                            part.Velocity = Vector3.zero
                            part.RotVelocity = Vector3.zero
                        end
                    end
                end
                local myHumanoid = character and character:FindFirstChild("Humanoid")
                if myHumanoid then
                    myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                end
                for _, seat in ipairs(Workspace:GetDescendants()) do
                    if seat:IsA("Seat") or seat:IsA("VehicleSeat") then
                        seat.Disabled = false
                    end
                end
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clothe1s"):FireServer("CharacterSizeUp", 1)
                    end
                )
                if flingToggle then
                    flingToggle:Set(false)
                end
            end
        end
    )
end
local function flingWithTruck(targetPlayer)
    if not targetPlayer or not targetPlayer.Character or not LocalPlayer.Character then
        return
    end
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local myHRP = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not myHRP then
        return
    end
    savedPosition = myHRP.Position
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(1181.83, 76.08, -1158.83))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    )
    task.wait(0.5)
    disableCarClient()
    local args = {[1] = "DeleteAllVehicles"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    args = {[1] = "PickingCar", [2] = "Semi"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    task.wait(1)
    local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
    if not vehiclesFolder then
        return
    end
    local truckName = LocalPlayer.Name .. "Car"
    local truck = vehiclesFolder:FindFirstChild(truckName)
    if not truck then
        return
    end
    pcall(
        function()
            myHRP.Anchored = true
            myHRP.CFrame = CFrame.new(Vector3.new(1176.56, 79.90, -1166.65))
            task.wait(0.2)
            myHRP.Velocity = Vector3.zero
            myHRP.RotVelocity = Vector3.zero
            myHRP.Anchored = false
            humanoid:ChangeState(Enum.HumanoidStateType.Seated)
        end
    )
    local sitStart = tick()
    repeat
        task.wait()
        if tick() - sitStart > 10 then
            return
        end
    until humanoid.Sit
    for _, part in ipairs(truck:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
            pcall(
                function()
                    part:SetNetworkOwner(nil)
                end
            )
        end
    end
    running = true
    connection =
        RunService.Stepped:Connect(
        function()
            if not running then
                return
            end
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    )
    local startTime = tick()
    local lastFlingTime = 0
    flingConnection =
        RunService.Heartbeat:Connect(
        function()
            if not running then
                return
            end
            if not targetPlayer or not targetPlayer.Character then
                running = false
                return
            end
            local newTargetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local newTargetHumanoid = targetPlayer.Character:FindFirstChild("Humanoid")
            if not newTargetHRP or not newTargetHumanoid then
                running = false
                return
            end
            if not myHRP or not humanoid then
                running = false
                return
            end
            local trailer = truck:FindFirstChild("Body") and truck.Body:FindFirstChild("Trailer")
            if not trailer then
                return
            end
            local verticalOffset = math.sin(tick() * 30) * 5
            pcall(
                function()
                    local targetPosition = newTargetHRP.Position + Vector3.new(0, verticalOffset, 0)
                    trailer:PivotTo(CFrame.new(targetPosition))
                end
            )
            local dist = (trailer.Position - newTargetHRP.Position).Magnitude
            if dist < 5 and tick() - lastFlingTime > 0.4 then
                lastFlingTime = tick()
                for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
                local fling = Instance.new("BodyVelocity")
                fling.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                fling.Velocity =
                    Vector3.new(math.random(-10, 10), 50, math.random(-10, 10)).Unit * 10000000 +
                    Vector3.new(0, 5000000, 0)
                fling.Parent = newTargetHRP
                task.delay(
                    0.5,
                    function()
                        fling:Destroy()
                        for _, part in ipairs(targetPlayer.Character:GetDescendants()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = true
                            end
                        end
                    end
                )
            end
            local playerSeated = false
            for _, seat in ipairs(truck:GetDescendants()) do
                if (seat:IsA("Seat") or seat:IsA("VehicleSeat")) and seat.Name ~= "VehicleSeat" then
                    if seat.Occupant == newTargetHumanoid then
                        playerSeated = true
                        break
                    end
                end
            end
            if playerSeated or tick() - startTime > 10 then
                running = false
                if connection then
                    connection:Disconnect()
                    connection = nil
                end
                if flingConnection then
                    flingConnection:Disconnect()
                    flingConnection = nil
                end
                pcall(
                    function()
                        truck:PivotTo(CFrame.new(Vector3.new(-59599.73, 2040070.50, -293391.16)))
                    end
                )
                task.wait(0.5)
                disableCarClient()
                local args = {[1] = "DeleteAllVehicles"}
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
                    end
                )
                if character then
                    local myHRP = character:FindFirstChild("HumanoidRootPart")
                    if myHRP and savedPosition then
                        pcall(
                            function()
                                myHRP.Anchored = true
                                myHRP.CFrame = CFrame.new(savedPosition + Vector3.new(0, 5, 0))
                                task.wait(0.2)
                                myHRP.Velocity = Vector3.zero
                                myHRP.RotVelocity = Vector3.zero
                                myHRP.Anchored = false
                                if humanoid then
                                    humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                                end
                            end
                        )
                    end
                end
                if character then
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = true
                            part.Velocity = Vector3.zero
                            part.RotVelocity = Vector3.zero
                        end
                    end
                end
                local myHumanoid = character and character:FindFirstChild("Humanoid")
                if myHumanoid then
                    myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                end
                for _, seat in ipairs(Workspace:GetDescendants()) do
                    if seat:IsA("Seat") or seat:IsA("VehicleSeat") then
                        seat.Disabled = false
                    end
                end
                pcall(
                    function()
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clothe1s"):FireServer("CharacterSizeUp", 1)
                    end
                )
                if flingToggle then
                    flingToggle:Set(false)
                end
            end
        end
    )
end
local function stopFling()
    running = false
    if connection then
        connection:Disconnect()
        connection = nil
    end
    if flingConnection then
        flingConnection:Disconnect()
        flingConnection = nil
    end
    if soccerBall then
        soccerBall.Anchored = originalProperties.Anchored
        soccerBall.CanCollide = originalProperties.CanCollide
        soccerBall.CanTouch = originalProperties.CanTouch
    end
    if couch and couch:IsA("BasePart") then
        couch.Anchored = originalProperties.Anchored
        couch.CanCollide = originalProperties.CanCollide
        couch.CanTouch = originalProperties.CanTouch
    end
    disableCarClient()
    local args = {[1] = "DeleteAllVehicles"}
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer(unpack(args))
        end
    )
    task.wait(0.2)
    local character = LocalPlayer.Character
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
                part.Velocity = Vector3.zero
                part.RotVelocity = Vector3.zero
            end
        end
    end
    local myHumanoid = character and character:FindFirstChild("Humanoid")
    if myHumanoid then
        myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    end
    for _, seat in ipairs(Workspace:GetDescendants()) do
        if seat:IsA("Seat") or seat:IsA("VehicleSeat") then
            seat.Disabled = false
        end
    end
    pcall(
        function()
            ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clothe1s"):FireServer("CharacterSizeUp", 1)
        end
    )
    if savedPosition then
        local myHRP = character and character:FindFirstChild("HumanoidRootPart")
        if myHRP then
            pcall(
                function()
                    myHRP.Anchored = true
                    myHRP.CFrame = CFrame.new(savedPosition + Vector3.new(0, 5, 0))
                    task.wait(0.2)
                    myHRP.Velocity = Vector3.zero
                    myHRP.RotVelocity = Vector3.zero
                    myHRP.Anchored = false
                    if myHumanoid then
                        myHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                    end
                end
            )
        end
    end
end
flingToggle =
    Tab9:AddToggle(
    {
        Name = "انواع فلنقات",
        Description = "",
        Default = false,
        Callback = function(state)
            if state then
                if isFollowingKill or isFollowingPull or running then
                    flingToggle:Set(false)
                    return
                end
                if selectedFlingMethod == "كنبه" then
                    flingWithSofa(selectedPlayer)
                elseif selectedFlingMethod == "كره" then
                    flingWithBall(selectedPlayer)
                elseif selectedFlingMethod == "كره V2 " then
                    flingWithBallV2(selectedPlayer)
                elseif selectedFlingMethod == "سفينه" then
                    flingWithBoat(selectedPlayer)
                elseif selectedFlingMethod == "تريله" then
                    flingWithTruck(selectedPlayer)
                elseif selectedFlingMethod == "باص" then
                    flingWithBus(selectedPlayer)
                end
            else
                stopFling()
            end
        end
    }
)
local Section = Tab9:AddSection({"اكتب كل لاعبين الضدك وضيفهم للقائمه ثم دوس فلنق راح ياثر بس عليهم"})
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local StarterGui = game:GetService("StarterGui")
orbitando = false
orbitConn = nil
allFling = false
allConn = nil
currentPlayerList = nil
currentPlayerIndex = nil
lastSwitchTime = nil
allFling2 = false
allConn2 = nil
soccerBall = nil
originalProperties = nil
excludedPlayers = {}
local function getPlayerThumbnail(userId)
    local thumbType = Enum.ThumbnailType.HeadShot
    local thumbSize = Enum.ThumbnailSize.Size420x420
    local success, result =
        pcall(
        function()
            return Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)
        end
    )
    if success then
        return result
    else
        warn("Erro ao obter thumbnail:" .. tostring(result))
        return nil
    end
end
local function findPlayerByPartialName(partialName)
    partialName = partialName:lower()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr.Name:lower():find(partialName) then
            return plr
        end
    end
    return nil
end
local function showNotification(title, description, icon)
    pcall(
        function()
            StarterGui:SetCore("SendNotification", {Title = title, Text = description, Icon = icon or "", Duration = 5})
        end
    )
end
Tab9:AddTextBox(
    {
        Name = "اضافه لاعب الى القائمه",
        Description = "",
        PlaceholderText = "اسم لاعب",
        Callback = function(Value)
            if Value == "" then
                showNotification("تم اضافه ب القائمه السوداء", "KIRA", nil)
                return
            end
            local player = findPlayerByPartialName(Value)
            if player then
                for _, excluded in ipairs(excludedPlayers) do
                    if excluded == player then
                        showNotification(
                            "Jogador Já esta na whaitelist",
                            "kiraaa " .. player.Name .. "",
                            getPlayerThumbnail(player.UserId)
                        )
                        return
                    end
                end
                table.insert(excludedPlayers, player)
                local thumbnail = getPlayerThumbnail(player.UserId)
                showNotification(
                    "my kira",                    "للتاكيد تم الاضافه" .. player.Name .. "",
                    thumbnail
                )
            else
                showNotification("هم يالله", "" .. Value .. "'.", nil)
            end
        end
    }
)
Tab9:AddButton(    {"اضافه لاعب للقائمه", function()
            if #excludedPlayers == 0 then
                showNotification("تم الاضافه", "مشمول ب فلنقات", nil)
                return
            end
            for i, player in ipairs(excludedPlayers) do
                local thumbnail = getPlayerThumbnail(player.UserId)
                showNotification(
                    " ",
                    "عيب والله تكتل بل وادم" .. player.Name .. "",
                    thumbnail
                )
                task.wait(0.5)
            end
        end}
)
Tab9:AddButton(
    {"شيل الاشياء المحذوفه", function()
            if #excludedPlayers == 0 then
                showNotification("تم الشيل", "KIRA", nil)
                return
            end
            excludedPlayers = {}
            showNotification("تم شيل الادوات", "", nil)
        end}
)
Tab9:AddButton(
    {
        "اشمر الكره",
        function()
            if orbitando then
                return
            end
            if not equipBola() then
                return
            end
            task.wait(0.5)
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local myHRP = character:FindFirstChild("HumanoidRootPart")
            if not myHRP then
                return
            end
            local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
            local soccerBalls = workspaceCom and workspaceCom:FindFirstChild("001_SoccerBalls")
            soccerBall = soccerBalls and soccerBalls:FindFirstChild("Soccer" .. LocalPlayer.Name)
            if not soccerBall then
                return
            end
            originalProperties = {
                Anchored = soccerBall.Anchored,
                CanCollide = soccerBall.CanCollide,
                CanTouch = soccerBall.CanTouch
            }
            soccerBall.Anchored = false
            soccerBall.CanCollide = true
            soccerBall.CanTouch = true
            pcall(
                function()
                    soccerBall:SetNetworkOwner(nil)
                end
            )
            orbitando = true
            orbitConn =
                RunService.Heartbeat:Connect(
                function()
                    if
                        not orbitando or not soccerBall or not soccerBall.Parent or not myHRP or not myHRP.Parent or
                            not character or
                            not character.Parent
                     then
                        if orbitConn then
                            orbitConn:Disconnect()
                            orbitConn = nil
                        end
                        orbitando = false
                        if soccerBall and originalProperties then
                            soccerBall.Anchored = originalProperties.Anchored
                            soccerBall.CanCollide = originalProperties.CanCollide
                            soccerBall.CanTouch = originalProperties.CanTouch
                        end
                        soccerBall = nil
                        originalProperties = nil
                        return
                    end
                    local t = tick() * 10
                    local radius = 3
                    local offset = Vector3.new(math.cos(t) * radius, -1, math.sin(t) * radius)
                    soccerBall.CFrame = CFrame.new(myHRP.Position + offset)
                    soccerBall.AssemblyLinearVelocity = Vector3.new(9999, 9999, 9999)
                end
            )
        end
    }
)
Tab9:AddButton(
    {
        "فلنق كره V1",
        function()
            if allFling then
                return
            end
            if not equipBola() then
                return
            end
            task.wait(0.5)
            local args = {[1] = "PlayerWantsToDeleteTool", [2] = "SoccerBall"}
            pcall(
                function()
                    ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))
                end
            )
            local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
            local soccerBalls = workspaceCom and workspaceCom:FindFirstChild("001_SoccerBalls")
            soccerBall = soccerBalls and soccerBalls:FindFirstChild("Soccer" .. LocalPlayer.Name)
            if not soccerBall then
                return
            end
            originalProperties = {
                Anchored = soccerBall.Anchored,
                CanCollide = soccerBall.CanCollide,
                CanTouch = soccerBall.CanTouch
            }
            soccerBall.Anchored = false
            soccerBall.CanCollide = true
            soccerBall.CanTouch = true
            pcall(
                function()
                    soccerBall:SetNetworkOwner(nil)
                end
            )
            allFling = true
            local function getShuffledPlayers()
                local playerList = {}
                for _, plr in ipairs(Players:GetPlayers()) do
                    local isExcluded = false
                    for _, excluded in ipairs(excludedPlayers) do
                        if plr == excluded then
                            isExcluded = true
                            break
                        end
                    end
                    if
                        plr ~= LocalPlayer and not isExcluded and plr.Character and
                            plr.Character:FindFirstChild("HumanoidRootPart")
                     then
                        table.insert(playerList, plr)
                    end
                end
                for i = #playerList, 2, -1 do
                    local j = math.random(i)
                    playerList[i], playerList[j] = playerList[j], playerList[i]
                end
                return playerList
            end
            allConn =
                RunService.Heartbeat:Connect(
                function()
                    if not allFling or not soccerBall or not soccerBall.Parent then
                        if allConn then
                            allConn:Disconnect()
                            allConn = nil
                        end
                        allFling = false
                        if soccerBall and originalProperties then
                            soccerBall.Anchored = originalProperties.Anchored
                            soccerBall.CanCollide = originalProperties.CanCollide
                            soccerBall.CanTouch = originalProperties.CanTouch
                        end
                        soccerBall = nil
                        originalProperties = nil
                        currentPlayerList = nil
                        currentPlayerIndex = nil
                        lastSwitchTime = nil
                        return
                    end
                    if not currentPlayerList or #currentPlayerList == 0 then
                        currentPlayerList = getShuffledPlayers()
                        currentPlayerIndex = 1
                        lastSwitchTime = tick()
                    end
                    if #currentPlayerList == 0 then
                        return
                    end
                    if tick() - lastSwitchTime >= 4 then
                        currentPlayerIndex = currentPlayerIndex + 1
                        if currentPlayerIndex > #currentPlayerList then
                            currentPlayerList = getShuffledPlayers()
                            currentPlayerIndex = 1
                        end
                        lastSwitchTime = tick()
                    end
                    local target = currentPlayerList[currentPlayerIndex]
                    if not target or not target.Character then
                        return
                    end
                    local targetChar = target.Character
                    if
                        targetChar and targetChar:FindFirstChild("HumanoidRootPart") and
                            targetChar:FindFirstChild("Humanoid")
                     then
                        local hrp = targetChar.HumanoidRootPart
                        local humanoid = targetChar.Humanoid
                        local velocity = hrp.Velocity
                        local speed = velocity.Magnitude
                        local isJumping = humanoid:GetState() == Enum.HumanoidStateType.Jumping
                        local isMoving = humanoid.MoveDirection.Magnitude > 0.05
                        local offset
                        if isMoving or isJumping then
                            local moveDir = hrp.CFrame.LookVector
                            local extraDist = math.clamp(speed / 1.5, 6, 18)
                            offset = moveDir * extraDist + Vector3.new(0, 1, 0)
                        else
                            local waveZ = math.sin(tick() * 25) * 4
                            local sideX = math.cos(tick() * 20) * 1.5
                            offset = Vector3.new(sideX, 1, waveZ)
                        end
                        soccerBall.CFrame = CFrame.new(hrp.Position + offset)
                        soccerBall.AssemblyLinearVelocity = Vector3.new(9999, 9999, 9999)
                        if (soccerBall.Position - hrp.Position).Magnitude < 4 then
                            local fling = Instance.new("BodyVelocity")
                            fling.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                            fling.Velocity =
                                Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 500000 +
                                Vector3.new(0, 250000, 0)
                            fling.Parent = hrp
                            task.delay(
                                0.3,
                                function()
                                    fling:Destroy()
                                end
                            )
                        end
                    end
                end
            )
        end
    }
)
Tab9:AddButton(
    {
        "فلنق كره V2",
        function()
            if allFling2 then
                return
            end
            if not equipBola() then
                return
            end
            task.wait(0.5)
            local args = {[1] = "PlayerWantsToDeleteTool", [2] = "SoccerBall"}
            pcall(
                function()
                    ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))
                end
            )
            local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
            local soccerBalls = workspaceCom and workspaceCom:FindFirstChild("001_SoccerBalls")
            soccerBall = soccerBalls and soccerBalls:FindFirstChild("Soccer" .. LocalPlayer.Name)
            if not soccerBall then
                return
            end
            originalProperties = {
                Anchored = soccerBall.Anchored,
                CanCollide = soccerBall.CanCollide,
                CanTouch = soccerBall.CanTouch
            }
            soccerBall.Anchored = false
            soccerBall.CanCollide = true
            soccerBall.CanTouch = true
            pcall(
            
                function()
                    soccerBall:SetNetworkOwner(nil)
                end
            )
            allFling2 = true
            allConn2 =
                RunService.Heartbeat:Connect(
                function()
                    if not allFling2 or not soccerBall or not soccerBall.Parent then
                        if allConn2 then
                            allConn2:Disconnect()
                            allConn2 = nil
                        end
                        allFling2 = false
                        if soccerBall and originalProperties then
                            soccerBall.Anchored = originalProperties.Anchored
                            soccerBall.CanCollide = originalProperties.CanCollide
                            soccerBall.CanTouch = originalProperties.CanTouch
                        end
                        soccerBall = nil
                        originalProperties = nil
                        return
                    end
                    local playerList = {}
                    for _, plr in ipairs(Players:GetPlayers()) do
                        local isExcluded = false
                        for _, excluded in ipairs(excludedPlayers) do
                            if plr == excluded then
                                isExcluded = true
                                break
                            end
                        end
                        if
                            plr ~= LocalPlayer and not isExcluded and plr.Character and
                                plr.Character:FindFirstChild("HumanoidRootPart")
                         then
                            table.insert(playerList, plr)
                        end
                    end
                    for i = #playerList, 2, -1 do
                        local j = math.random(i)
                        playerList[i], playerList[j] = playerList[j], playerList[i]
                    end
                    for _, target in ipairs(playerList) do
                        if not allFling2 then
                            break
                        end
                        local targetChar = target.Character
                        if
                            targetChar and targetChar:FindFirstChild("HumanoidRootPart") and
                                targetChar:FindFirstChild("Humanoid")
                         then
                            local hrp = targetChar.HumanoidRootPart
                            local humanoid = targetChar.Humanoid
                            local velocity = hrp.Velocity
                            local speed = velocity.Magnitude
                            local isJumping = humanoid:GetState() == Enum.HumanoidStateType.Jumping
                            local isMoving = humanoid.MoveDirection.Magnitude > 0.05
                            local offset
                            if isMoving or isJumping then
                                local moveDir = hrp.CFrame.LookVector
                                local extraDist = math.clamp(speed / 1.5, 6, 18)
                                offset = moveDir * extraDist + Vector3.new(0, 1, 0)
                            else
                                local waveZ = math.sin(tick() * 25) * 4
                                local sideX = math.cos(tick() * 20) * 1.5
                                offset = Vector3.new(sideX, 1, waveZ)
                            end
                            soccerBall.CFrame = CFrame.new(hrp.Position + offset)
                            soccerBall.AssemblyLinearVelocity = Vector3.new(9999, 9999, 9999)
                            if (soccerBall.Position - hrp.Position).Magnitude < 4 then
                                local fling = Instance.new("BodyVelocity")
                                fling.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                                fling.Velocity =
                                    Vector3.new(math.random(-5, 5), 5, math.random(-5, 5)).Unit * 1000000 +
                                    Vector3.new(0, 1000000, 0)
                                fling.Parent = hrp
                                task.delay(
                                    0.3,
                                    function()
                                        fling:Destroy()
                                    end
                                )
                            end
                        end
                        task.wait(0.1)
                    end
                end
            )
        end
    }
)
Tab9:AddButton(
    {"وكف كلشي", function()
            orbitando = false
            if orbitConn then
                orbitConn:Disconnect()
                orbitConn = nil
            end
            allFling = false
            if allConn then
                allConn:Disconnect()
                allConn = nil
            end
            currentPlayerList = nil
            currentPlayerIndex = nil
            lastSwitchTime = nil
            allFling2 = false
            if allConn2 then
                allConn2:Disconnect()
                allConn2 = nil
            end
            if soccerBall and originalProperties then
                soccerBall.Anchored = originalProperties.Anchored
                soccerBall.CanCollide = originalProperties.CanCollide
                soccerBall.CanTouch = originalProperties.CanTouch
            end
            soccerBall = nil
            originalProperties = nil
            showNotification("تم ايقاف كل شي", "الحمد لله كلت هاذ مايعوفهم", nil)
        end}
)
local Section = Tab9:AddSection({"ادوات فلنق"})

Tab9:AddButton(
    {
    Name = "اداة فلنق كنبة",
    Callback = function()

local jogadores = game:GetService("Players")
local rep = game:GetService("ReplicatedStorage")
local entrada = game:GetService("UserInputService")
local eu = jogadores.LocalPlayer
local cam = workspace.CurrentCamera

local podeClicar = true
local ferramentaEquipada = false
local NOME_FERRAMENTA = "Click Fling Couch"

local mochila = eu:WaitForChild("Backpack")

if not mochila:FindFirstChild(NOME_FERRAMENTA) and not (eu.Character and eu.Character:FindFirstChild(NOME_FERRAMENTA)) then
	local ferramenta = Instance.new("Tool")
	ferramenta.Name = NOME_FERRAMENTA
	ferramenta.RequiresHandle = false
	ferramenta.CanBeDropped = false

	ferramenta.Equipped:Connect(function()
		ferramentaEquipada = true
	end)

	ferramenta.Unequipped:Connect(function()
		ferramentaEquipada = false
	end)

	ferramenta.Parent = mochila
end

local function jogarComSofa(alvo)
	if not ferramentaEquipada then return end
	if not alvo or not alvo.Character or alvo == eu then return end

	local jogando = true
	local raiz = eu.Character and eu.Character:FindFirstChild("HumanoidRootPart")
	local alvoRaiz = alvo.Character and alvo.Character:FindFirstChild("HumanoidRootPart")
	if not raiz or not alvoRaiz then return end

	local boneco = eu.Character
	local humano = boneco:FindFirstChildOfClass("Humanoid")
	local posOriginal = raiz.CFrame

	rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllTools")
	task.wait(0.2)

	rep.RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "Couch")
	task.wait(0.3)

	local sofa = eu.Backpack:FindFirstChild("Couch")
	if sofa then
		sofa.Parent = boneco
	end
	task.wait(0.1)

	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.F, false, game)
	task.wait(0.25)

	workspace.FallenPartsDestroyHeight = 0/0

	local forca = Instance.new("BodyVelocity")
	forca.Name = "ForcaJogada"
	forca.Velocity = Vector3.new(9e8, 9e8, 9e8)
	forca.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	forca.Parent = raiz

	humano:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
	humano.PlatformStand = false
	cam.CameraSubject = alvo.Character:FindFirstChild("Head") or alvoRaiz or humano

	task.spawn(function()
		local angulo = 0
		local partes = {raiz}
		while jogando and alvo and alvo.Character and alvo.Character:FindFirstChildOfClass("Humanoid") do
			local alvoHum = alvo.Character:FindFirstChildOfClass("Humanoid")
			if alvoHum.Sit then break end
			angulo += 50

			for _, parte in ipairs(partes) do
				local hrp = alvo.Character.HumanoidRootPart
				local pos = hrp.Position + hrp.Velocity / 1.5
				raiz.CFrame = CFrame.new(pos) * CFrame.Angles(math.rad(angulo), 0, 0)
			end

			raiz.Velocity = Vector3.new(9e8, 9e8, 9e8)
			raiz.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
			task.wait()
		end

		jogando = false
		forca:Destroy()
		humano:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
		humano.PlatformStand = false
		raiz.CFrame = posOriginal
		cam.CameraSubject = humano

		for _, p in pairs(boneco:GetDescendants()) do
			if p:IsA("BasePart") then
				p.Velocity = Vector3.zero
				p.RotVelocity = Vector3.zero
			end
		end

		humano:UnequipTools()
		rep.RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "Couch")
	end)

	while jogando do
		task.wait()
	end
end

entrada.TouchTap:Connect(function(toques, processado)
	if processado or not podeClicar or not ferramentaEquipada then return end

	local pos = toques[1]
	local raio = cam:ScreenPointToRay(pos.X, pos.Y)
	local acerto = workspace:Raycast(raio.Origin, raio.Direction * 1000)

	if acerto and acerto.Instance then
		local alvo = jogadores:GetPlayerFromCharacter(acerto.Instance:FindFirstAncestorOfClass("Model"))
		if alvo and alvo ~= eu then
			podeClicar = false
			jogarComSofa(alvo)
			task.delay(2, function()
				podeClicar = true
			end)
		end
	end
end)
end
})

Tab9:AddButton(
    {
    Name = "اداة فلنق كرة",
    Callback = function()
local jogadores = game:GetService("Players")
local rep = game:GetService("ReplicatedStorage")
local mundo = game:GetService("Workspace")
local entrada = game:GetService("UserInputService")
local cam = mundo.CurrentCamera
local eu = jogadores.LocalPlayer

local NOME_FERRAMENTA = "Click Fling Ball"
local ferramentaEquipada = false

local mochila = eu:WaitForChild("Backpack")
if not mochila:FindFirstChild(NOME_FERRAMENTA) then
	local ferramenta = Instance.new("Tool")
	ferramenta.Name = NOME_FERRAMENTA
	ferramenta.RequiresHandle = false
	ferramenta.CanBeDropped = false

	ferramenta.Equipped:Connect(function()
		ferramentaEquipada = true
	end)

	ferramenta.Unequipped:Connect(function()
		ferramentaEquipada = false
	end)

	ferramenta.Parent = mochila
end

-- FunÃ§Ã£o FlingBall (bola)
local function FlingBall(target)
    

    local players = game:GetService("Players")
    local player = players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local hrp = character:WaitForChild("HumanoidRootPart")
    local backpack = player:WaitForChild("Backpack")
    local ServerBalls = workspace.WorkspaceCom:WaitForChild("001_SoccerBalls")

    local function GetBall()
        if not backpack:FindFirstChild("SoccerBall") and not character:FindFirstChild("SoccerBall") then
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "SoccerBall")
        end

        repeat task.wait() until backpack:FindFirstChild("SoccerBall") or character:FindFirstChild("SoccerBall")

        local ballTool = backpack:FindFirstChild("SoccerBall")
        if ballTool then
            ballTool.Parent = character
        end

        repeat task.wait() until ServerBalls:FindFirstChild("Soccer" .. player.Name)

        return ServerBalls:FindFirstChild("Soccer" .. player.Name)
    end

    local Ball = ServerBalls:FindFirstChild("Soccer" .. player.Name) or GetBall()
    Ball.CanCollide = false
    Ball.Massless = true
    Ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

    if target ~= player then
        local tchar = target.Character
        if tchar and tchar:FindFirstChild("HumanoidRootPart") and tchar:FindFirstChild("Humanoid") then
            local troot = tchar.HumanoidRootPart
            local thum = tchar.Humanoid

            if Ball:FindFirstChildWhichIsA("BodyVelocity") then
                Ball:FindFirstChildWhichIsA("BodyVelocity"):Destroy()
            end

            local bv = Instance.new("BodyVelocity")
            bv.Name = "FlingPower"
            bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.P = 9e900
            bv.Parent = Ball

            workspace.CurrentCamera.CameraSubject = thum

            repeat
                if troot.Velocity.Magnitude > 0 then
                    local pos = troot.Position + (troot.Velocity / 1.5)
                    Ball.CFrame = CFrame.new(pos)
                    Ball.Orientation += Vector3.new(45, 60, 30)
                else
                    for i, v in pairs(tchar:GetChildren()) do
                        if v:IsA("BasePart") and v.CanCollide and not v.Anchored then
                            Ball.CFrame = v.CFrame
                            task.wait(1/6000)
                        end
                    end
                end
                task.wait(1/6000)
            until troot.Velocity.Magnitude > 1000 or thum.Health <= 0 or not tchar:IsDescendantOf(workspace) or target.Parent ~= players

            workspace.CurrentCamera.CameraSubject = humanoid
        end
    end
end

-- Toque na tela para aplicar a bola
entrada.TouchTap:Connect(function(toques, processado)
	if not ferramentaEquipada or processado then return end
	local pos = toques[1]
	local raio = cam:ScreenPointToRay(pos.X, pos.Y)
	local hit = mundo:Raycast(raio.Origin, raio.Direction * 1000)
	if hit and hit.Instance then
		local modelo = hit.Instance:FindFirstAncestorOfClass("Model")
		local jogador = jogadores:GetPlayerFromCharacter(modelo)
		if jogador and jogador ~= eu then
			FlingBall(jogador)
		end
	end
end)

end
})

Tab9:AddButton(
    {
    Name = "اداء فلنق ادمن",
    Callback = function()
        local jogadores = game:GetService("Players")
        local rep = game:GetService("ReplicatedStorage")
        local mundo = game:GetService("Workspace")
        local entrada = game:GetService("UserInputService")
        local cam = mundo.CurrentCamera
        local eu = jogadores.LocalPlayer

        local NOME_FERRAMENTA = "Admin Fling"
        local ferramentaEquipada = false

        local mochila = eu:WaitForChild("Backpack")

        for _, ferramentaExistente in pairs(mochila:GetChildren()) do
            if ferramentaExistente:IsA("Tool") and ferramentaExistente.Name:lower():find("fling") then
                ferramentaExistente.Name = "Admin Fling"
            end
        end

        if not mochila:FindFirstChild(NOME_FERRAMENTA) then
            local ferramenta = Instance.new("Tool")
            ferramenta.Name = NOME_FERRAMENTA
            ferramenta.RequiresHandle = true
            ferramenta.CanBeDropped = false

            local handle = Instance.new("Part")
            handle.Name = "Handle"
            handle.Size = Vector3.new(1, 1, 1)
            handle.Transparency = 1
            handle.Parent = ferramenta

            local decal = Instance.new("Decal")
            decal.Texture = "rbxassetid://775552544"
            decal.Face = Enum.NormalId.Front
            decal.Parent = handle

            ferramenta.Equipped:Connect(function()
                ferramentaEquipada = true
            end)

            ferramenta.Unequipped:Connect(function()
                ferramentaEquipada = false
            end)

            ferramenta.Parent = mochila
        end

        local function FlingBall(target)
            local player = jogadores.LocalPlayer
            local character = player.Character or player.CharacterAdded:Wait()
            local humanoid = character:WaitForChild("Humanoid")
            local hrp = character:WaitForChild("HumanoidRootPart")
            local backpack = player:WaitForChild("Backpack")
            local ServerBalls = mundo:WaitForChild("WorkspaceCom"):WaitForChild("001_SoccerBalls")

            local function GetBall()
                if not backpack:FindFirstChild("SoccerBall") and not character:FindFirstChild("SoccerBall") then
                    rep.RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "SoccerBall")
                end
                repeat task.wait() until backpack:FindFirstChild("SoccerBall") or character:FindFirstChild("SoccerBall")
                local ballTool = backpack:FindFirstChild("SoccerBall")
                if ballTool then ballTool.Parent = character end
                repeat task.wait() until ServerBalls:FindFirstChild("Soccer" .. player.Name)
                return ServerBalls:FindFirstChild("Soccer" .. player.Name)
            end

            local Ball = ServerBalls:FindFirstChild("Soccer" .. player.Name) or GetBall()
            Ball.CanCollide = false
            Ball.Massless = true
            Ball.Transparency = 1             -- BOLA INVISÃVEL
            Ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

            if target ~= player then
                local tchar = target.Character
                if tchar and tchar:FindFirstChild("HumanoidRootPart") and tchar:FindFirstChild("Humanoid") then
                    local troot = tchar.HumanoidRootPart
                    local thum = tchar.Humanoid
                    if Ball:FindFirstChildWhichIsA("BodyVelocity") then
                        Ball:FindFirstChildWhichIsA("BodyVelocity"):Destroy()
                    end
                    local bv = Instance.new("BodyVelocity")
                    bv.Name = "FlingPower"
                    bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
                    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    bv.P = 9e900
                    bv.Parent = Ball
                    mundo.CurrentCamera.CameraSubject = thum

                    repeat
                        if troot.Velocity.Magnitude > 0 then
                            local pos = troot.Position + (troot.Velocity / 1.5)
                            Ball.CFrame = CFrame.new(pos)
                            Ball.Orientation += Vector3.new(45, 60, 30)
                        else
                            for _, v in pairs(tchar:GetChildren()) do
                                if v:IsA("BasePart") and v.CanCollide and not v.Anchored then
                                    Ball.CFrame = v.CFrame
                                    task.wait(1/6000)
                                end
                            end
                        end
                        task.wait(1/6000)
                    until troot.Velocity.Magnitude > 1000 or thum.Health <= 0 or not tchar:IsDescendantOf(mundo) or target.Parent ~= jogadores

                    mundo.CurrentCamera.CameraSubject = humanoid
                end
            end
        end

        entrada.TouchTap:Connect(function(toques, processado)
            if not ferramentaEquipada or processado then return end
            local pos = toques[1]
            local raio = cam:ScreenPointToRay(pos.X, pos.Y)
            local hit = mundo:Raycast(raio.Origin, raio.Direction * 1000)
            if hit and hit.Instance then
                local modelo = hit.Instance:FindFirstAncestorOfClass("Model")
                local jogador = jogadores:GetPlayerFromCharacter(modelo)
                if jogador and jogador ~= eu then
                    FlingBall(jogador)
                end
            end
        end)

    end
})



   Tab10:AddParagraph({"تخريب جو"}) 
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local AvatarManager = {}
AvatarManager.ReplicatedStorage = ReplicatedStorage
function AvatarManager:MostrarNotificacao(mensagem)
    pcall(
        function()
            game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Aviso", Text = mensagem, Duration = 5})
        end
    )
end
AvatarManager.Avatares = {
    {Nome = "كوكيد", ID = 85461264894562},
    {Nome = "رصاصي", ID = 7683459677},
    {Nome = "رقمي اسود", ID = 9936028435},
    {Nome = "اسود مع تفطرات خضراء ", ID = 4888539621},
    {Nome = "هالوين", ID = 6074054267 },
    {Nome = "جمجمه", ID = 300169816},
      {Nome = "اخضر و اسود", ID = 5417858215 },
    {Nome = "جمجمه خضره", ID = 99419498829832},
    {Nome = "تنين اسود", ID = 105670722939866},
    {Nome = "ابيض و احمر ", ID = 18214204122},
    {Nome = "ابيض و اسود", ID = 15990913226 },
    {Nome = "اخضر و اسود مثل لغه برمجه", ID = 9003199127},
        {Nome = "دموي", ID = 7799682401},
    {Nome = "رصاصي مع اخضر", ID = 14942013416},
    {Nome = "ابو سجين", ID = 6866730483},
    {Nome = "ابيض موشم", ID = 11607314013},
    {Nome = "اصفر", ID = 14132962406 },
    {Nome = "ريالي", ID = 13716378674},
      {Nome = "هالوين ثاني", ID = 5790341430 },
    {Nome = "ابيض بيه كتابات", ID = 14836989155},
    {Nome = "زومبي ", ID = 128034574943237},
    {Nome = "احمر", ID = 14747772616 },

}
function AvatarManager:GetAvatarNames()
    local nomes = {}
    for _, avatar in ipairs(self.Avatares) do
        table.insert(nomes, avatar.Nome)
    end
    return nomes
end
function AvatarManager:EquiparAvatar(avatarName)
    for _, avatar in ipairs(self.Avatares) do
        if avatar.Nome == avatarName then
            local args = {avatar.ID}
            local success, result =
                pcall(
                function()
                    return self.ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Wear"):InvokeServer(
                        unpack(args)
                    )
                end
            )
            if success then
                self:MostrarNotificacao("Avatar " .. avatarName .. " equipado com sucesso!")
            else
                self:MostrarNotificacao("Falha ao equipar o avatar " .. avatarName .. "!")
            end
            return
        end
    end
    self:MostrarNotificacao("Avatar " .. avatarName .. " não encontrado!")
end
local AvatarDropdown =
    Tab10:AddDropdown(
    {
        Name = "اختر سكن من هنا وابدي تخري",
        Description = "",
        Default = nil,
        Options = AvatarManager:GetAvatarNames(),
        Callback = function(avatarSelecionado)
            _G.SelectedAvatar = avatarSelecionado
        end
    }
)
Tab10:AddButton({
    Name = "تخريب الجو",
    Description = "by BRUTON",
    Callback = function()
        -- أولاً: equipar
        if not _G.SelectedAvatar or _G.SelectedAvatar == "" then
            AvatarManager:MostrarNotificacao("Nenhum avatar selecionado!")
        else
            AvatarManager:EquiparAvatar(_G.SelectedAvatar)
        end

        -- ثانياً: تفعيل تخريب الجو
        local Players = game:GetService("Players")
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local LocalPlayer = Players.LocalPlayer

        local Remotes = ReplicatedStorage:WaitForChild("Remotes")
        local WearRemote = Remotes:WaitForChild("Wear")
        local ChangeCharacterBody = Remotes:WaitForChild("ChangeCharacterBody")
        local WearShirtRemote = Remotes:WaitForChild("WearShirt")

        local CurrentTrack
        local BodyId = "100839513065432"
        local SkyboxActive = false
        local Humanoid, Animator

        local function SetupCharacter(char)
            Humanoid = char:WaitForChild("Humanoid")
            Animator = Humanoid:FindFirstChildOfClass("Animator")
            if not Animator then
                Animator = Instance.new("Animator", Humanoid)
            end
        end
        
        SetupCharacter(LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait())
        LocalPlayer.CharacterAdded:Connect(SetupCharacter)

        local function ApplyAccessories()
            local Desc = Humanoid:GetAppliedDescription()
            for _, accessory in ipairs(Desc:GetAccessories(true)) do
                if accessory.AssetId then
                    pcall(function()
                        WearRemote:InvokeServer(accessory.AssetId)
                    end)
                end
            end
        end

        local function ApplyBody()
            pcall(function()
                ChangeCharacterBody:InvokeServer({
                    [1] = tonumber(BodyId),
                    [2] = nil,
                    [3] = nil,
                    [4] = nil,
                    [5] = nil,
                    [6] = nil
                })
            end)
        end

        local function PlayAnimation()
            local AnimId = "rbxassetid://101852027997337"
            local newAnim = Instance.new("Animation")
            newAnim.AnimationId = AnimId

            local newTrack
            pcall(function()
                newTrack = Animator:LoadAnimation(newAnim)
                newTrack.Priority = Enum.AnimationPriority.Action4
                newTrack:Play(0.1, 1, 1)
            end)

            CurrentTrack = newTrack
        end

        local function StopAnimation()
            if CurrentTrack then
                pcall(function()
                    CurrentTrack:Stop(0)
                end)
            end
        end

        SkyboxActive = not SkyboxActive
        if SkyboxActive then
            local id = tonumber(getgenv().SkyboxClothID or "")
            if id then
                pcall(function()
                    WearShirtRemote:InvokeServer(id)
                end)
            end
            ApplyAccessories()
            ApplyBody()
            PlayAnimation()
            print("تم equipar وتفعيل Skybox مع الملابس ✅")
        else
            StopAnimation()
            print("تم إيقاف Skybox")
        end
    end
})



  local Section = Tab10:AddSection({"تقفل سيرفر", "wifi"})
Tab10:AddButton(
    {
Name = "تقفيل سيرفر V1", 
    Callback = function()
  tools = "FireX"
          shutdownserver = true
          if game.Players.LocalPlayer.Character.Humanoid.Sit == true then
              task.wait()
              game.Players.LocalPlayer.Character.Humanoid.Sit = false
          end
          if game:GetService("Workspace"):FindFirstChild("Camera") then
              game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
          end
          wait(0.1)
          if game:GetService("Workspace"):FindFirstChild("Camera") then
              game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
          end
          game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = game:GetService("Workspace").WorkspaceCom["001_GiveTools"].FireX.CFrame + Vector3.new(0, -15, 0)
          task.wait(0.2)
          game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
          ddos = true
          for i = 1, 1350 do
              task.wait()
              if ddos == false then
                  local args = {
                      [1] = "ClearAllTools"
                  }
   
                  cleartoolremote:FireServer(unpack(args))
                  game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
                  game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(9999, -475, 9999)
                  return
              end
              if game:GetService("Workspace"):FindFirstChild("Camera") then
                  game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
              end
              if game:GetService("Players").LocalPlayer.Character:FindFirstChild(tools) then
                  game:GetService("Players").LocalPlayer.Character:FindFirstChild(tools):Destroy()
              end
              if ddos == false then return end
              fireclickdetector(game:GetService("Workspace").WorkspaceCom["001_GiveTools"].FireX.ClickDetector, 0)
          end
          game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
          game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(0, -475, 0)
    end
  })

  Tab10:AddButton(
    {
  Name = "تقفيل سيرفر V2", 
    Callback = function()
  tools = "FireX"
          shutdownserver = true
          if game.Players.LocalPlayer.Character.Humanoid.Sit == true then
              task.wait()
              game.Players.LocalPlayer.Character.Humanoid.Sit = false
          end
          if game:GetService("Workspace"):FindFirstChild("Camera") then
              game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
          end
          wait(0.1)
          if game:GetService("Workspace"):FindFirstChild("Camera") then
              game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
          end
          game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = game:GetService("Workspace").WorkspaceCom["001_GiveTools"].FireX.CFrame + Vector3.new(0, -15, 0)
          task.wait(0.2)
          game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
          ddos = true
          for i = 1, 1350 do
              task.wait()
              if ddos == false then
                  local args = {
                      [1] = "ClearAllTools"
                  }
   
                  cleartoolremote:FireServer(unpack(args))
                  game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
                  game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(9999, -475, 9999)
                  return
              end
              if game:GetService("Workspace"):FindFirstChild("Camera") then
                  game:GetService("Workspace"):FindFirstChild("Camera"):Destroy()
              end
              if game:GetService("Players").LocalPlayer.Character:FindFirstChild(tools) then
                  game:GetService("Players").LocalPlayer.Character:FindFirstChild(tools):Destroy()
              end
              if ddos == false then return end
              fireclickdetector(game:GetService("Workspace").WorkspaceCom["001_GiveTools"].FireX.ClickDetector, 0)
          end
          game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
          game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(0, -475, 0)
    end
  })
  
  local Section = Tab10:AddSection({"حدد سرعه سفينه"})
Tab10:AddTextBox({
Name = "سرعة السفينة",
Description = "اكتب السرعة المطلوبة للسفينة",
Placeholder = "مثلاً 500",
Default = "500",
Callback = function(text)
local num = tonumber(text)
if num and num > 0 then
SpeedValue = num
print("✅ تم ضبط سرعة السفينة إلى:", SpeedValue)
else
warn("❌ الرجاء إدخال رقم صحيح للسرعة.")
end
end
})

Tab10:AddToggle({
Name = "حط سفينه",
Description = "زر لتشغيل أو إزالة السفينة، تتحرك بالسرعة اللي تختارها من العداد فوق",
Default = false,
Callback = function(state)
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Player = game.Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")
local Vehicles = workspace:WaitForChild("Vehicles")

    if state then
        local function spawnBoat()
            RootPart.CFrame = CFrame.new(1754, -2, 58)
            task.wait(0.5)
            RS:WaitForChild("RE"):FindFirstChild("1Ca1r"):FireServer("PickingBoat", "PirateFree")
            task.wait(1)
            return Vehicles:FindFirstChild(Player.Name .. "Car")
        end

        local PCar = spawnBoat()
        if not PCar then return warn("فشل إنشاء السفينة") end

        local Seat = PCar:FindFirstChild("Body") and PCar.Body:FindFirstChild("VehicleSeat")
        if not Seat then return warn("Seat غير موجود") end

        repeat task.wait(0.1)
            RootPart.CFrame = Seat.CFrame * CFrame.new(0, 1, 0)
        until Humanoid.SeatPart == Seat

        -- قلب السفينة كل 0.1 ثانية
        local RE_Flip = RS:WaitForChild("RE"):WaitForChild("1Player1sCa1r")
        task.spawn(function()
            while PCar and PCar.Parent and state do
                RE_Flip:FireServer("Flip")
                task.wait(0.5)
            end
        end)


        local waypoints = {
            Vector3.new(-16, 0, -47),
            Vector3.new(-110, 0, -45),
            Vector3.new(16, 0, -55)
        }

        local currentIndex, nextIndex, progress, currentRotation = 1, 2, 0, 0
        local moveSpeed = SpeedValue
        local rotationSpeed = math.rad(720)

        local function lerpCFrame(a, b, t)
            return a:lerp(b, t)
        end

        RunService.Heartbeat:Connect(function(dt)
            if not state or not (PCar and PCar.PrimaryPart) then return end
            local startPos = waypoints[currentIndex]
            local endPos = waypoints[nextIndex]
            progress += (moveSpeed * dt) / (startPos - endPos).Magnitude
            if progress >= 1 then
                progress = 0
                currentIndex = nextIndex
                nextIndex = (nextIndex % #waypoints) + 1
            end
            local newPos = lerpCFrame(CFrame.new(startPos), CFrame.new(endPos), progress).p
            currentRotation += rotationSpeed * dt
            local cf = CFrame.new(newPos) * CFrame.Angles(0, currentRotation, 0)
            PCar:SetPrimaryPartCFrame(cf)
        end)

    else
        local success, err = pcall(function()
            RS:WaitForChild("RE"):WaitForChild("1Ca1r"):FireServer("DeleteAllVehicles")
        end)
        if success then
            print(" تم حذف السفينة بنجاح")
        else
            warn(" فشل حذف السفينة:", err)
        end
    end
end
})

Tab11:AddButton({"شادر ليلي", function()
    local workspace = game:GetService("Workspace")
    local Lighting = game:GetService("Lighting")
    local RunService = game:GetService("RunService")
    local Debris = game:GetService("Debris")
    local TweenService = game:GetService("TweenService")
    local SoundService = game:GetService("SoundService")
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer

    -- 🔊 صوت التفعيل
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://131644923"
    sound.Volume = 1
    sound.Parent = SoundService
    sound:Play()

    -- 🔹 تطبيق المواد على الخرائط (Basalt وCobblestone)
    local model = workspace:FindFirstChild("Model")
    if model then
        local function setMat(obj)
            for _, c in pairs(obj:GetChildren()) do
                if c:IsA("BasePart") then
                    c.Material = Enum.Material.Basalt
                elseif c:IsA("Model") or c:IsA("Folder") then
                    setMat(c)
                end
            end
        end
        if model:FindFirstChild("001_SnowStreet") then setMat(model["001_SnowStreet"]) end
        if model:FindFirstChild("Street") then
            for _, o in pairs(model.Street:GetDescendants()) do
                if o:IsA("BasePart") then o.Material = Enum.Material.Basalt end
            end
        end
        for _, o in pairs(model:GetChildren()) do
            if o:IsA("BasePart") and (o.Name=="Sidewalk" or o.Name=="Wedge") and o.Material==Enum.Material.SmoothPlastic then
                o.Material = Enum.Material.Cobblestone
            end
        end
        model.ChildAdded:Connect(function(obj)
            if obj:IsA("BasePart") and (obj.Name=="Sidewalk" or obj.Name=="Wedge") and obj.Material==Enum.Material.SmoothPlastic then
                obj.Material = Enum.Material.Cobblestone
            end
        end)
    end

    -- 🔊 إعداد أصوات البيئة
    local character = player.Character or player.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart")

    local soundPart = Instance.new("Part")
    soundPart.Size = Vector3.new(1,1,1)
    soundPart.Transparency = 1
    soundPart.Anchored = true
    soundPart.CanCollide = false
    soundPart.Parent = workspace

    local birdSound = Instance.new("Sound")
    birdSound.Name = "BirdsSound"
    birdSound.SoundId = "rbxassetid://1237969272"
    birdSound.Looped = true
    birdSound.Volume = 0.05
    birdSound.Parent = soundPart

    local wolfSound = Instance.new("Sound")
    wolfSound.SoundId = "rbxassetid://6654360741"
    wolfSound.Volume = 0.05
    wolfSound.Looped = false
    wolfSound.Parent = workspace

    RunService.Heartbeat:Connect(function()
        if hrp and hrp.Parent then
            soundPart.Position = hrp.Position + Vector3.new(0,10,0)
        end
    end)

    local function isNight()
        local t = Lighting.ClockTime
        return (t >= 18 or t <= 6)
    end

    task.spawn(function()
        while true do
            if isNight() then
                if birdSound.IsPlaying then birdSound:Stop() end
                if wolfSound.IsPlaying then wolfSound:Stop() end
                wolfSound:Play()
            else
                if wolfSound.IsPlaying then wolfSound:Stop() end
                if not birdSound.IsPlaying then birdSound:Play() end
            end
            wait(20)
        end
    end)

    -- 🔊 نافورة
    local fountainPart = Instance.new("Part")
    fountainPart.Anchored = true
    fountainPart.CanCollide = false
    fountainPart.Transparency = 1
    fountainPart.Size = Vector3.new(1,1,1)
    fountainPart.Position = Vector3.new(-27,19,15)
    fountainPart.Parent = workspace

    local attachment = Instance.new("Attachment")
    attachment.Position = Vector3.new(-27,19,15)
    attachment.Parent = fountainPart

    local fountainSound = Instance.new("Sound")
    fountainSound.Name = "FountainSound"
    fountainSound.SoundId = "rbxassetid://4766793559"
    fountainSound.Looped = true
    fountainSound.Volume = 0.03
    fountainSound.EmitterSize = 10
    fountainSound.RollOffMode = Enum.RollOffMode.Linear
    fountainSound.MaxDistance = 100
    fountainSound.Parent = attachment
    fountainSound:Play()

    -- 🔊 صوت مخصص
    local customSound = Instance.new("Sound")
    customSound.Name = "MyCustomSound"
    customSound.SoundId = "rbxassetid://9048659736"
    customSound.Volume = 0.01
    customSound.Looped = true
    customSound.Parent = workspace
    customSound:Play()

    -- 🔹 النجوم والنجوم الطائرة
    local active = false
    local stars = {}
    local shootingStarsFolder = Instance.new("Folder",workspace)
    shootingStarsFolder.Name = "ShootingStars"
    local STAR_COUNT = 300
    local SHOOTING_STAR_CHANCE = 0.3
    local SHOOTING_STAR_MAX = 12
    local shootingStarCooldown = 0.1

    local spaceSound = Instance.new("Sound",workspace)
    spaceSound.SoundId = "rbxassetid://1843520836"
    spaceSound.Volume = 0.3
    spaceSound.Looped = true
    spaceSound.Name = "SpaceAmbience"

    local function createStar()
        local star = Instance.new("Part")
        local size = math.random(1,3)*0.5
        star.Size = Vector3.new(size,size,size)
        star.Position = Vector3.new(math.random(-1000,1000),math.random(300,700),math.random(-1000,1000))
        star.Anchored = true
        star.CanCollide = false
        star.Material = Enum.Material.Neon
        local colors = {Color3.fromRGB(255,255,255),Color3.fromRGB(255,255,180),Color3.fromRGB(180,200,255)}
        star.Color = colors[math.random(1,#colors)]
        star.Name = "Star"
        star.Parent = workspace
        local light = Instance.new("PointLight",star)
        light.Brightness = 2 + math.random()*1.5
        light.Range = 12
        spawn(function()
            while star.Parent and active do
                star.Transparency = 0.2 + math.sin(tick()*math.random(2,5))*0.2
                RunService.Heartbeat:Wait()
            end
            if star.Parent then star:Destroy() end
        end)
        table.insert(stars,star)
    end

    local function createShootingStar()
        if not active then return end
        local startPos = Vector3.new(math.random(-1000,1000),math.random(350,600),math.random(-1000,1000))
        local dir = Vector3.new(math.random(-1,1),math.random(-0.1,0.1),math.random(-1,1)).Unit
        local speed = math.random(350,550)
        local isFire = math.random() <= SHOOTING_STAR_CHANCE
        local color = isFire and Color3.fromRGB(255,50,50) or Color3.fromRGB(255,255,220)
        local trailColor = isFire and ColorSequence.new(Color3.fromRGB(255,120,0),Color3.fromRGB(255,230,50)) or ColorSequence.new(Color3.fromRGB(255,255,255),Color3.fromRGB(255,255,180))
        local star = Instance.new("Part")
        star.Size = Vector3.new(0.5,0.5,3)
        star.Position = startPos
        star.Anchored = true
        star.CanCollide = false
        star.Material = Enum.Material.Neon
        star.Color = color
        star.Name = "ShootingStar"
        star.Parent = shootingStarsFolder
        local att0 = Instance.new("Attachment",star)
        local att1 = Instance.new("Attachment",star)
        att1.Position = Vector3.new(0,0,-3)
        local trail = Instance.new("Trail",star)
        trail.Attachment0 = att0
        trail.Attachment1 = att1
        trail.Lifetime = 0.35
        trail.Color = trailColor
        trail.LightEmission = 1
        trail.WidthScale = NumberSequence.new({NumberSequenceKeypoint.new(0,1),NumberSequenceKeypoint.new(1,0)})
        local light = Instance.new("PointLight",star)
        light.Brightness = isFire and 12 or 7
        light.Range = 35
        light.Color = color
        if isFire then
            local fire = Instance.new("Fire",star)
            fire.Heat = 15
            fire.Size = 3.5
            fire.Color = Color3.fromRGB(255,110,0)
            fire.SecondaryColor = Color3.fromRGB(255,210,0)
        end
        local lifetime = math.random(1,1.5)
        local timePassed = 0
        local moveConn
        moveConn = RunService.Heartbeat:Connect(function(dt)
            if not active then moveConn:Disconnect() if star.Parent then star:Destroy() end return end
            timePassed += dt
            if timePassed >= lifetime then moveConn:Disconnect() if star.Parent then star:Destroy() end return end
            local curve = math.sin(timePassed*20)*0.5
            star.Position += (dir+Vector3.new(0,curve,0)).Unit*speed*dt
        end)
        Debris:AddItem(star,4)
    end

    local function updateSky()
        local hour = Lighting.ClockTime
        local shouldBeActive = hour >= 18 or hour < 6
        if shouldBeActive and not active then
            active = true
            Lighting.FogColor = Color3.fromRGB(10,10,30)
            Lighting.FogEnd = 5000
            Lighting.Brightness = 2
            for _,s in ipairs(stars) do if s and s.Parent then s:Destroy() end end
            stars = {}
            for _,p in ipairs(shootingStarsFolder:GetChildren()) do p:Destroy() end
            for i=1,STAR_COUNT do createStar() end
            spaceSound:Play()
        elseif not shouldBeActive and active then
            active = false
            for _,s in ipairs(stars) do if s and s.Parent then s:Destroy() end end
            stars = {}
            for _,p in ipairs(shootingStarsFolder:GetChildren()) do p:Destroy() end
            spaceSound:Stop()
            Lighting.FogColor = Color3.fromRGB(192,192,192)
            Lighting.FogEnd = 100000
            Lighting.Brightness = 2
        end
    end

    task.spawn(function()
        while true do
            if active then
                for i=1,SHOOTING_STAR_MAX do
                    createShootingStar()
                    task.wait(shootingStarCooldown)
                end
            else
                task.wait(1)
            end
        end
    end)

    task.spawn(function()
        while true do
            updateSky()
            task.wait(1)
        end
    end)

    -- 🔹 المطر والبرق
    local rainFolder = Instance.new("Folder",workspace)
    rainFolder.Name = "FakeRain"
    local isRaining = false

    local birds = Instance.new("Sound",SoundService)
    birds.SoundId = "rbxassetid://9111139882"
    birds.Volume = 0.2
    birds.Looped = true
    birds:Play()

    local rainSound = Instance.new("Sound",SoundService)
    rainSound.SoundId = "rbxassetid://9118823106"
    rainSound.Volume = 0.3
    rainSound.Looped = true
    rainSound:Play()

    local thunder = Instance.new("Sound",SoundService)
    thunder.SoundId = "rbxassetid://9120018695"
    thunder.Volume = 0.4

    local function updateBirdSound()
        birds.Volume = isRaining and 0 or 0.2
    end

    local function spawnRain()
        isRaining = true
        updateBirdSound()
        for i=1,120 do
            local drop = Instance.new("Part")
            drop.Size = Vector3.new(0.1,2,0.1)
            drop.Anchored = true
            drop.CanCollide = false
            drop.Material = Enum.Material.Glass
            drop.Transparency = 0.5
            drop.Color = Color3.fromRGB(160,160,255)
            drop.Position = Vector3.new(math.random(-150,150),100,math.random(-150,150))
            drop.Parent = rainFolder
            local tween = TweenService:Create(drop,TweenInfo.new(1),{Position=drop.Position-Vector3.new(0,60,0)})
            tween:Play()
            Debris:AddItem(drop,1.5)
        end
        wait(1.5)
        isRaining = false
        updateBirdSound()
    end

    local function lightningStrike()
        local flash = Instance.new("Part")
        flash.Size = Vector3.new(1,1000,1)
        flash.Anchored = true
        flash.CanCollide = false
        flash.Transparency = 0.4
        flash.Material = Enum.Material.Neon
        flash.Color = Color3.new(1,1,1)
        flash.Position = Vector3.new(math.random(-100,100),500,math.random(-100,100))
        flash.Parent = workspace
        Lighting.Brightness = Lighting.Brightness + 1.5
        thunder:Play()
        wait(0.1)
        Lighting.Brightness = Lighting.Brightness - 1.5
        flash:Destroy()
    end

    task.spawn(function()
        while true do
            spawnRain()
            if math.random() < 0.2 then lightningStrike() end
            wait(1)
        end
    end)

    -- 🔹 الجو الليلي الإضافي
    local ccNight = Instance.new("ColorCorrectionEffect")
    ccNight.Name = "Moon_CC"
    ccNight.Parent = Lighting
    ccNight.Brightness = -0.05
    ccNight.Contrast = 0.1
    ccNight.Saturation = -0.3
    ccNight.TintColor = Color3.fromRGB(160,180,255)

    Lighting.FogStart = 0
    Lighting.FogEnd = 150
    Lighting.FogColor = Color3.fromRGB(120,140,200)
    Lighting.Brightness = 0.5
    Lighting.Ambient = Color3.fromRGB(70,80,110)

    print("✅ تم تفعيل الشادر الكامل مع الجو الليلي بنجاح")
end})

Tab11:AddButton({"شادر ب جو صباح ", function()
    -- Aviso: script otimizado, ativaÃ§Ã£o automÃ¡tica sem interface grÃ¡fica.

local workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local Debris = game:GetService("Debris")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local model = workspace:FindFirstChild("Model")

-- Som de ativaÃ§Ã£o
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://131644923"
sound.Volume = 1
sound.Parent = SoundService
sound:Play()

-- Aplicar materiais ao mapa
if model then
	local function setMat(obj)
		for _, c in pairs(obj:GetChildren()) do
			if c:IsA("BasePart") then
				c.Material = Enum.Material.Basalt
			elseif c:IsA("Model") or c:IsA("Folder") then
				setMat(c)
			end
		end
	end
	if model:FindFirstChild("001_SnowStreet") then
		setMat(model["001_SnowStreet"])
	end
	if model:FindFirstChild("Street") then
		for _, o in pairs(model.Street:GetDescendants()) do
			if o:IsA("BasePart") then
				o.Material = Enum.Material.Basalt
			end
		end
	end
	for _, o in pairs(model:GetChildren()) do
		if o:IsA("BasePart") and (o.Name == "Sidewalk" or o.Name == "Wedge") and o.Material == Enum.Material.SmoothPlastic then
			o.Material = Enum.Material.Cobblestone
		end
	end
	model.ChildAdded:Connect(function(obj)
		if obj:IsA("BasePart") and (obj.Name == "Sidewalk" or obj.Name == "Wedge") and obj.Material == Enum.Material.SmoothPlastic then
			obj.Material = Enum.Material.Cobblestone
		end
	end)
end

local soundPart = Instance.new("Part")
soundPart.Size = Vector3.new(1,1,1)
soundPart.Transparency = 1
soundPart.Anchored = true
soundPart.CanCollide = false
soundPart.Parent = workspace
local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")

local birdSound = Instance.new("Sound")
birdSound.Name = "BirdsSound"
birdSound.SoundId = "rbxassetid://1237969272"
birdSound.Looped = true
birdSound.Volume = 0.05
birdSound.Parent = soundPart
local wolfSound = Instance.new("Sound")
wolfSound.SoundId = "rbxassetid://6654360741"
wolfSound.Volume = 0.05
wolfSound.Looped = false
wolfSound.Parent = workspace
RunService.Heartbeat:Connect(function()
	if hrp and hrp.Parent then
		soundPart.Position = hrp.Position + Vector3.new(0,10,0)
	end
end)
local function isNight()
	local t = Lighting.ClockTime
	return (t >= 18 or t <= 6)
end
task.spawn(function()
	while true do
		if isNight() then
			if birdSound.IsPlaying then birdSound:Stop() end
			if wolfSound.IsPlaying then wolfSound:Stop() end
			wolfSound:Play()
		else
			if wolfSound.IsPlaying then wolfSound:Stop() end
			if not birdSound.IsPlaying then birdSound:Play() end
		end
		wait(20)
	end
end)

local fountainPart = Instance.new("Part")
fountainPart.Anchored = true
fountainPart.CanCollide = false
fountainPart.Transparency = 1
fountainPart.Size = Vector3.new(1,1,1)
fountainPart.Position = Vector3.new(-27,19,15)
fountainPart.Parent = workspace
local attachment = Instance.new("Attachment")
attachment.Position = Vector3.new(-27,19,15)
attachment.Parent = fountainPart
local fountainSound = Instance.new("Sound")
fountainSound.Name = "FountainSound"
fountainSound.SoundId = "rbxassetid://4766793559"
fountainSound.Looped = true
fountainSound.Volume = 0.03
fountainSound.EmitterSize = 10
fountainSound.RollOffMode = Enum.RollOffMode.Linear
fountainSound.MaxDistance = 100
fountainSound.Parent = attachment
fountainSound:Play()
local customSound = Instance.new("Sound")
customSound.Name = "MyCustomSound"
customSound.SoundId = "rbxassetid://9048659736"
customSound.Volume = 0.01
customSound.Looped = true
customSound.PlayOnRemove = false
customSound.Parent = workspace
customSound:Play()
local active = false
local stars = {}
local shootingStarsFolder = Instance.new("Folder",workspace)
shootingStarsFolder.Name = "ShootingStars"
local STAR_COUNT = 300
local SHOOTING_STAR_CHANCE = 0.3
local SHOOTING_STAR_MAX = 12
local shootingStarCooldown = 0.1
local spaceSound = Instance.new("Sound",workspace)
spaceSound.SoundId = "rbxassetid://1843520836"
spaceSound.Volume = 0.3
spaceSound.Looped = true
spaceSound.Name = "SpaceAmbience"
local function createStar()
	local star = Instance.new("Part")
	local size = math.random(1,3)*0.5
	star.Size = Vector3.new(size,size,size)
	star.Position = Vector3.new(math.random(-1000,1000),math.random(300,700),math.random(-1000,1000))
	star.Anchored = true
	star.CanCollide = false
	star.Material = Enum.Material.Neon
	local colors = {Color3.fromRGB(255,255,255),Color3.fromRGB(255,255,180),Color3.fromRGB(180,200,255)}
	star.Color = colors[math.random(1,#colors)]
	star.Name = "Star"
	star.Parent = workspace
	local light = Instance.new("PointLight",star)
	light.Brightness = 2 + math.random()*1.5
	light.Range = 12
	spawn(function()
		while star.Parent and active do
			star.Transparency = 0.2 + math.sin(tick()*math.random(2,5))*0.2
			RunService.Heartbeat:Wait()
		end
		if star.Parent then star:Destroy() end
	end)
	table.insert(stars,star)
end
local function createShootingStar()
	if not active then return end
	local startPos = Vector3.new(math.random(-1000,1000),math.random(350,600),math.random(-1000,1000))
	local dir = Vector3.new(math.random(-1,1),math.random(-0.1,0.1),math.random(-1,1)).Unit
	local speed = math.random(350,550)
	local isFire = math.random() <= SHOOTING_STAR_CHANCE
	local color = isFire and Color3.fromRGB(255,50,50) or Color3.fromRGB(255,255,220)
	local trailColor = isFire and ColorSequence.new(Color3.fromRGB(255,120,0),Color3.fromRGB(255,230,50)) or ColorSequence.new(Color3.fromRGB(255,255,255),Color3.fromRGB(255,255,180))
	local star = Instance.new("Part")
	star.Size = Vector3.new(0.5,0.5,3)
	star.Position = startPos
	star.Anchored = true
	star.CanCollide = false
	star.Material = Enum.Material.Neon
	star.Color = color
	star.Name = "ShootingStar"
	star.Parent = shootingStarsFolder
	local att0 = Instance.new("Attachment",star)
	local att1 = Instance.new("Attachment",star)
	att1.Position = Vector3.new(0,0,-3)
	local trail = Instance.new("Trail",star)
	trail.Attachment0 = att0
	trail.Attachment1 = att1
	trail.Lifetime = 0.35
	trail.Color = trailColor
	trail.LightEmission = 1
	trail.WidthScale = NumberSequence.new({NumberSequenceKeypoint.new(0,1),NumberSequenceKeypoint.new(1,0)})
	local light = Instance.new("PointLight",star)
	light.Brightness = isFire and 12 or 7
	light.Range = 35
	light.Color = color
	if isFire then
		local fire = Instance.new("Fire",star)
		fire.Heat = 15
		fire.Size = 3.5
		fire.Color = Color3.fromRGB(255,110,0)
		fire.SecondaryColor = Color3.fromRGB(255,210,0)
	end
	local lifetime = math.random(1,1.5)
	local timePassed = 0
	local moveConn
	moveConn = RunService.Heartbeat:Connect(function(dt)
		if not active then moveConn:Disconnect() if star.Parent then star:Destroy() end return end
		timePassed += dt
		if timePassed >= lifetime then moveConn:Disconnect() if star.Parent then star:Destroy() end return end
		local curve = math.sin(timePassed*20)*0.5
		star.Position += (dir+Vector3.new(0,curve,0)).Unit*speed*dt
	end)
	Debris:AddItem(star,4)
end
local function updateSky()
	local hour = Lighting.ClockTime
	local shouldBeActive = hour >= 18 or hour < 6
	if shouldBeActive and not active then
		active = true
		Lighting.FogColor = Color3.fromRGB(10,10,30)
		Lighting.FogEnd = 5000
		Lighting.Brightness = 2
		for _,s in ipairs(stars) do if s and s.Parent then s:Destroy() end end
		stars = {}
		for _,p in ipairs(shootingStarsFolder:GetChildren()) do p:Destroy() end
		for i=1,STAR_COUNT do createStar() end
		spaceSound:Play()
	elseif not shouldBeActive and active then
		active = false
		for _,s in ipairs(stars) do if s and s.Parent then s:Destroy() end end
		stars = {}
		for _,p in ipairs(shootingStarsFolder:GetChildren()) do p:Destroy() end
		spaceSound:Stop()
		Lighting.FogColor = Color3.fromRGB(192,192,192)
		Lighting.FogEnd = 100000
		Lighting.Brightness = 2
	end
end
task.spawn(function()
	while true do
		if active then
			for i=1,SHOOTING_STAR_MAX do
				createShootingStar()
				task.wait(shootingStarCooldown)
			end
		else
			task.wait(1)
		end
	end
end)
task.spawn(function()
	while true do
		updateSky()
		task.wait(1)
	end
end)
local rainFolder = Instance.new("Folder",workspace)
rainFolder.Name = "FakeRain"
local isRaining = false

local birds = Instance.new("Sound",SoundService)
birds.SoundId = "rbxassetid://9111139882"
birds.Volume = 0.2
birds.Looped = true
birds:Play()

local rainSound = Instance.new("Sound",SoundService)
rainSound.SoundId = "rbxassetid://9118823106"
rainSound.Volume = 0.3
rainSound.Looped = true
rainSound:Play()

local thunder = Instance.new("Sound",SoundService)
thunder.SoundId = "rbxassetid://9120018695"
thunder.Volume = 0.4

local function updateBirdSound()
	birds.Volume = isRaining and 0 or 0.2
end

local function spawnRain()
	isRaining = true
	updateBirdSound()
	for i=1,120 do
		local drop = Instance.new("Part")
		drop.Size = Vector3.new(0.1,2,0.1)
		drop.Anchored = true
		drop.CanCollide = false
		drop.Material = Enum.Material.Glass
		drop.Transparency = 0.5
		drop.Color = Color3.fromRGB(160,160,255)
		drop.Position = Vector3.new(math.random(-150,150),100,math.random(-150,150))
		drop.Parent = rainFolder
		local tween = TweenService:Create(drop,TweenInfo.new(1),{Position=drop.Position-Vector3.new(0,60,0)})
		tween:Play()
		Debris:AddItem(drop,1.5)
	end
	wait(1.5)
	isRaining = false
	updateBirdSound()
end

local function lightningStrike()
	local flash = Instance.new("Part")
	flash.Size = Vector3.new(1,1000,1)
	flash.Anchored = true
	flash.CanCollide = false
	flash.Transparency = 0.4
	flash.Material = Enum.Material.Neon
	flash.Color = Color3.new(1,1,1)
	flash.Position = Vector3.new(math.random(-100,100),500,math.random(-100,100))
	flash.Parent = workspace
	Lighting.Brightness = Lighting.Brightness + 1.5
	thunder:Play()
	wait(0.1)
	Lighting.Brightness = Lighting.Brightness - 1.5
	flash:Destroy()
end

for _,part in pairs(workspace:GetDescendants()) do
	if part:IsA("BasePart") and part.Material == Enum.Material.SmoothPlastic then
		part.Reflectance = 0.25
	end
end

task.spawn(function()
	while true do
		spawnRain()
		if math.random() < 0.2 then lightningStrike() end
		wait(1)
	end
end)

-- IluminaÃ§Ã£o e ambiente geral
Lighting.Brightness = 2
Lighting.GlobalShadows = true
Lighting.OutdoorAmbient = Color3.fromRGB(70, 70, 70)
Lighting.FogColor = Color3.fromRGB(120, 130, 140)
Lighting.FogStart = 80
Lighting.FogEnd = 600
Lighting.EnvironmentSpecularScale = 1
Lighting.EnvironmentDiffuseScale = 0.5

local sky = Instance.new("Sky")
sky.SkyboxBk = "rbxassetid://159454299"
sky.SkyboxDn = "rbxassetid://159454296"
sky.SkyboxFt = "rbxassetid://159454293"
sky.SkyboxLf = "rbxassetid://159454286"
sky.SkyboxRt = "rbxassetid://159454300"
sky.SkyboxUp = "rbxassetid://159454304"
sky.Parent = Lighting

local color = Instance.new("ColorCorrectionEffect", Lighting)
color.Brightness = 0.03
color.Contrast = 0.15
color.Saturation = 0.05
color.TintColor = Color3.fromRGB(255, 240, 220)

local bloom = Instance.new("BloomEffect", Lighting)
bloom.Intensity = 0.8
bloom.Size = 56
bloom.Threshold = 0.9

local sunRays = Instance.new("SunRaysEffect", Lighting)
sunRays.Intensity = 0.05
sunRays.Spread = 0.8

local blur = Instance.new("BlurEffect", Lighting)
blur.Size = 0
end})
local Section = Tab11:AddSection({"شادرات قابله للاطفاء"})
Tab11:AddToggle({Name="جو ليلي",Default=false,Callback=function(a)local b=game:GetService("Lighting")local c="Moon_"if a then local d=Instance.new("ColorCorrectionEffect")d.Name=c.."CC"d.Parent=b;d.Brightness=-0.05;d.Contrast=0.1;d.Saturation=-0.3;d.TintColor=Color3.fromRGB(160,180,255)b.FogStart=0;b.FogEnd=150;b.FogColor=Color3.fromRGB(120,140,200)b.Brightness=0.5;b.Ambient=Color3.fromRGB(70,80,110)else if b:FindFirstChild(c.."CC")then b[c.."CC"]:Destroy()end;b.FogEnd=100000;b.FogColor=Color3.new(1,1,1)b.Brightness=2;b.Ambient=Color3.fromRGB(127,127,127)end end})Tab11:AddToggle({Name="جو مرعب",Default=false,Callback=function(a)local b=game:GetService("Lighting")local e=game:GetService("Players")local f=e.LocalPlayer;local g="ScaryAtmos_"if a then if not b:FindFirstChild(g.."OriginalValues")then local h=Instance.new("StringValue")h.Name=g.."OriginalValues"local i=b.Ambient;local j=b.OutdoorAmbient;local k=b.FogStart or 0;local l=b.FogEnd or 100000;local m=b.FogColor or Color3.new(1,1,1)h.Value=string.format("%f,%f,%f,%f,%f,%f,%f,%f,%f,%f,%f",i.R,i.G,i.B,j.R,j.G,j.B,k,l,m.R,m.G,m.B)h.Parent=b end;b.Ambient=Color3.fromRGB(45,45,55)b.OutdoorAmbient=Color3.fromRGB(25,25,35)b.Brightness=0.5;b.FogStart=0;b.FogEnd=120;b.FogColor=Color3.fromRGB(25,25,40)local d=Instance.new("ColorCorrectionEffect")d.Name=g.."ColorCorrection"d.Parent=b;d.Brightness=-0.05;d.Contrast=0.15;d.Saturation=-0.4;d.TintColor=Color3.fromRGB(120,100,120)local n=f.Character or f.CharacterAdded:Wait()local o=Instance.new("PointLight")o.Name=g.."PlayerLight"o.Range=20;o.Brightness=2;o.Color=Color3.fromRGB(255,240,200)o.Parent=n:WaitForChild("HumanoidRootPart")local p=Instance.new("BloomEffect")p.Name=g.."Bloom"p.Parent=b;p.Intensity=0.4;p.Size=22;p.Threshold=0.9 else for q,r in ipairs({"ColorCorrection","Bloom"})do local s=b:FindFirstChild(g..r)if s then s:Destroy()end end;local n=f.Character;if n and n:FindFirstChild("HumanoidRootPart")then local o=n.HumanoidRootPart:FindFirstChild(g.."PlayerLight")if o then o:Destroy()end end;if b:FindFirstChild(g.."OriginalValues")then local t=b[g.."OriginalValues"]local u,v=pcall(function()return t.Value end)if u and type(v)=="string"then local w={}for x in string.gmatch(v,"[^,]+")do table.insert(w,x)end;if#w>=11 then b.Ambient=Color3.new(tonumber(w[1]),tonumber(w[2]),tonumber(w[3]))b.OutdoorAmbient=Color3.new(tonumber(w[4]),tonumber(w[5]),tonumber(w[6]))b.FogStart=tonumber(w[7])b.FogEnd=tonumber(w[8])b.FogColor=Color3.new(tonumber(w[9]),tonumber(w[10]),tonumber(w[11]))end end;t:Destroy()end end end})Tab11:AddToggle({Name="جو ترابي",Default=false,Callback=function(a)local b=game:GetService("Lighting")local c="Sunset_"if a then local d=Instance.new("ColorCorrectionEffect")d.Name=c.."CC"d.Parent=b;d.Brightness=0.1;d.Contrast=0.2;d.Saturation=0.4;d.TintColor=Color3.fromRGB(255,150,100)b.FogStart=0;b.FogEnd=200;b.FogColor=Color3.fromRGB(255,180,120)b.Brightness=1;b.Ambient=Color3.fromRGB(255,160,120)else if b:FindFirstChild(c.."CC")then b[c.."CC"]:Destroy()end;b.FogEnd=100000;b.FogColor=Color3.new(1,1,1)b.Brightness=2;b.Ambient=Color3.fromRGB(127,127,127)end end})Tab11:AddToggle({Name="جو ضبابي",Default=false,Callback=function(a)local b=game:GetService("Lighting")local c="RainFog_"if a then local d=Instance.new("ColorCorrectionEffect")d.Name=c.."CC"d.Parent=b;d.Brightness=-0.15;d.Contrast=0.1;d.Saturation=-0.5;d.TintColor=Color3.fromRGB(100,100,120)b.FogStart=0;b.FogEnd=80;b.FogColor=Color3.fromRGB(70,70,90)b.Brightness=0.4;b.Ambient=Color3.fromRGB(60,60,70)else if b:FindFirstChild(c.."CC")then b[c.."CC"]:Destroy()end;b.FogEnd=100000;b.FogColor=Color3.new(1,1,1)b.Brightness=2;b.Ambient=Color3.fromRGB(127,127,127)end end})local y=Tab11:AddSection({"شادرات 3D"})local z=0.2;local A=false;Tab11:AddSlider({Name="سرعة سبام السيارات",Min=0,Max=1,Default=0.2,Increment=0.1,Callback=function(B)z=math.floor(B*10+0.5)/10 end})Tab11:AddToggle({Name="سبام سيارات",Default=false,Callback=function(a)A=a;if A then task.spawn(function()while A do local C=game:GetObjects("rbxassetid://8714153713")[1]C.Parent=workspace;C:MoveTo(game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:GetPivot().Position+Vector3.new(0,5,0)or Vector3.zero)task.wait(z)end end)end end})local y=ChildTab:AddSection({Name="لازم تحط طفل ودوس ارسال"})local D=game.Players.LocalPlayer;local E={}local function F()E={}for q,G in pairs(game.Players:GetPlayers())do table.insert(E,G.Name)end;return E end;F()ChildTab:AddParagraph({"ملاحضة:","تقدر ترسل طفل مزعج لاي لاعب."})local H=nil;local I=ChildTab:AddDropdown({Name="اختيار اللاعب لارسال الطفل",Options=E,Default="",Callback=function(J)if game.Players:FindFirstChild(J)then H=J else H=nil end end})ChildTab:AddButton({Name="تحديث القائمة",Callback=function()local K=F()if I and K then pcall(function()I:Refresh(K)end)if H and not game.Players:FindFirstChild(H)then H=nil;pcall(function()I:Set("")end)end end end})game.Players.PlayerAdded:Connect(function()task.wait(0.1)local K=F()if I and K then pcall(function()I:Refresh(K)end)end end)game.Players.PlayerRemoving:Connect(function(G)task.wait(0.1)local K=F()if I and K then pcall(function()I:Refresh(K)end)if H==G.Name then H=nil;pcall(function()I:Set("")end)end end end)ChildTab:AddButton({Name="ارسال الطفل",Callback=function()if not H then warn("Nenhum jogador selecionado!")return end;if not workspace:FindFirstChild(D.Name)or not workspace[D.Name]:FindFirstChild("FollowCharacter")then local L={[1]="CharacterFollowSpawnPlayer",[2]="BabyBoy"}local M,N=pcall(function()game:GetService("ReplicatedStorage").RE:FindFirstChild("1Bab1yFollo1w"):FireServer(unpack(L))end)if not M then warn("Erro ao enviar crianÃ§a: "..N)end end;task.wait(0.2)if workspace:FindFirstChild(D.Name)then for q,O in pairs(workspace[D.Name]:GetChildren())do if O:IsA("BasePart")then O.CanCollide=true end end end;local P=H;if workspace:FindFirstChild(P)and workspace:FindFirstChild(D.Name)and workspace[D.Name]:FindFirstChild("FollowCharacter")then workspace[D.Name].FollowCharacter.Parent=workspace[P]if rawget(getgenv(),"RunService")then return end;getgenv().RunService=game:GetService("RunService").Heartbeat:Connect(function()local Q=workspace[P]:FindFirstChild("FollowCharacter")if Q and Q:FindFirstChild("Torso")and Q.Torso:FindFirstChild("BodyPosition")then local R=workspace[P]:FindFirstChild("HumanoidRootPart")if R then Q.Torso.BodyPosition.Position=R.Position-R.CFrame.LookVector*3;Q.Torso.BodyGyro.CFrame=R.CFrame end end end)end end})ChildTab:AddButton({Name="ارجاع الطفل",Callback=function()if rawget(getgenv(),"RunService")then getgenv().RunService:Disconnect()getgenv().RunService=nil end;local L={[1]="DeleteFollowCharacter"}local M,N=pcall(function()game:GetService("ReplicatedStorage").RE:FindFirstChild("1Bab1yFollo1w"):FireServer(unpack(L))end)if not M then warn("Erro ao retornar crianÃ§a: "..N)end;local S={[1]="CharacterFollowSpawnPlayer",[2]="BabyBoy"}M,N=pcall(function()game:GetService("ReplicatedStorage").RE:FindFirstChild("1Bab1yFollo1w"):FireServer(unpack(S))end)if not M then warn("Erro ao spawnar crianÃ§a: "..N)end end})ChildTab:AddToggle({Name="مشاهدة اللاعب",Default=false,Callback=function(T)local e=game:GetService("Players")local U=game:GetService("RunService")local f=e.LocalPlayer;local V=workspace.CurrentCamera;if T then if not H then warn("Nenhum jogador selecionado para spectar!")return false end;if not rawget(getgenv(),"CameraConnection")then getgenv().CameraConnection=U.Heartbeat:Connect(function()local W=e:FindFirstChild(H)if W and W.Character and W.Character:FindFirstChild("Humanoid")then V.CameraSubject=W.Character.Humanoid else if rawget(getgenv(),"CameraConnection")then getgenv().CameraConnection:Disconnect()getgenv().CameraConnection=nil end;V.CameraSubject=f.Character and f.Character:FindFirstChild("Humanoid")or nil end end)end else if rawget(getgenv(),"CameraConnection")then getgenv().CameraConnection:Disconnect()getgenv().CameraConnection=nil end;if f.Character and f.Character:FindFirstChild("Humanoid")then V.CameraSubject=f.Character.Humanoid end end end})local X=25;Tab12:AddSlider({Name="مسافة التنقل",MinValue=10,MaxValue=50,Default=25,Increase=1,Callback=function(T)X=T end})Tab12:AddButton({Name="اداة تنقل",Callback=function()local G=game.Players.LocalPlayer;local Y=G:GetMouse()local Z=Instance.new("Tool")Z.RequiresHandle=false;Z.Name="اداة تنقل"Z.Activated:Connect(function()local _=Y.Hit.Position+Vector3.new(0,X/10,0)G.Character.HumanoidRootPart.CFrame=CFrame.new(_.X,_.Y,_.Z)end)Z.Parent=G.Backpack end})local y=Tab12:AddSection({"تنقل اماكن"})local a0={["امام البيوت"]=Vector3.new(0,0,0),["مكان الأسلحة"]=Vector3.new(-119,-28,235),["البيت المهجور"]=Vector3.new(986,4,63),["المكان السري"]=Vector3.new(672,4,-296),["اسفل الارض1"]=Vector3.new(505,-75,143),["عند المدرسة"]=Vector3.new(-312,4,211),["عند الكوفي"]=Vector3.new(161,8,52),["باللوبي"]=Vector3.new(-26,4,-23),["عند المستشفى"]=Vector3.new(-309,4,71),["اسفل الارض2"]=Vector3.new(-343,4,-613),["عند الجزيرة"]=Vector3.new(-1925,23,127),["عند المطار"]=Vector3.new(310,5,31),["فوق الجسر"]=Vector3.new(-589,141,-59),["مكان المولدة"]=Vector3.new(179,4,-464)}local a1=nil;Tab12:AddDropdown({Name="اختر المكان",Description="اختر المكان الذي تريد الذهاب إليه",Options=(function()local a2={}for a3,q in pairs(a0)do table.insert(a2,a3)end;return a2 end)(),Callback=function(B)a1=B end})Tab12:AddButton({Name="اذهب إلى المكان",Description="ينقلك إلى المكان المختار في القائمة",Callback=function()if a1 and a0[a1]then game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame=CFrame.new(a0[a1])else warn("الرجاء اختيار مكان أولاً!")end end})local y=Tab12:AddSection({"تنقلات بيوت"})local a4={["بيت 1"]=Vector3.new(260.29,4.37,209.32),["بيت 2"]=Vector3.new(234.49,4.37,228.00),["بيت 3"]=Vector3.new(262.79,21.37,210.84),["بيت 4"]=Vector3.new(229.60,21.37,225.40),["بيت 5"]=Vector3.new(173.44,21.37,228.11),["بيت 6"]=Vector3.new(-43,21,-137),["بيت 7"]=Vector3.new(-40,36,-137),["بيت 11"]=Vector3.new(-21,40,436),["بيت 12"]=Vector3.new(155,37,433),["بيت 13"]=Vector3.new(255,35,431),["بيت 14"]=Vector3.new(254,38,394),["بيت 15"]=Vector3.new(148,39,387),["بيت 16"]=Vector3.new(-17,42,395),["بيت 17"]=Vector3.new(-189,37,-247),["بيت 18"]=Vector3.new(-354,37,-244),["بيت 19"]=Vector3.new(-456,36,-245),["بيت 20"]=Vector3.new(-453,38,-295),["بيت 21"]=Vector3.new(-356,38,-294),["بيت 22"]=Vector3.new(-187,37,-295),["بيت 23"]=Vector3.new(-410,68,-447),["بيت 24"]=Vector3.new(-348,69,-496),["بيت 28"]=Vector3.new(-103,12,1087),["بيت 29"]=Vector3.new(-730,6,808),["بيت 30"]=Vector3.new(-245,7,822),["بيت 31"]=Vector3.new(639,76,-361),["بيت 32"]=Vector3.new(-908,6,-361),["بيت 33"]=Vector3.new(-111,70,-417),["بيت 34"]=Vector3.new(230,38,569),["بيت 35"]=Vector3.new(-30,13,2209)}local a5={}for a6,q in pairs(a4)do table.insert(a5,a6)end;table.sort(a5,function(a7,a8)local a9=tonumber(a7:match("%d+"))or 0;local aa=tonumber(a8:match("%d+"))or 0;return a9<aa end)local ab=nil;pcall(function()Tab12:AddDropdown({Name="اختيار البيت",Options=a5,Callback=function(ac)ab=ac end})end)pcall(function()Tab12:AddButton({Name="تنقل",Callback=function()if ab then local G=game.Players.LocalPlayer;if G and G.Character then G.Character.HumanoidRootPart.CFrame=CFrame.new(a4[ab])end else print("يرجى اختيار البيت أولًا!")end end})end)pcall(function()Tab12:AddLabel("اختر البيت ثم اضغط زر التلي بورتر")end)